# Architecture Overview: Social Features

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         SwiftUI Views                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐ │
│  │  FollowButton    │  │ ProfilePicture   │  │  Followers   │ │
│  │                  │  │    Picker        │  │   ListView   │ │
│  │  • Auto status   │  │                  │  │              │ │
│  │  • Animations    │  │  • PhotosPicker  │  │  • Followers │ │
│  │  • Loading       │  │  • Preview       │  │  • Following │ │
│  └──────────────────┘  │  • Upload        │  │  • Search    │ │
│           │             └──────────────────┘  └──────────────┘ │
│           │                      │                    │         │
└───────────┼──────────────────────┼────────────────────┼─────────┘
            │                      │                    │
            ▼                      ▼                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                       SocialService                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Follow/Unfollow          Profile Pictures      Social Lists   │
│  ┌──────────────┐        ┌──────────────┐      ┌────────────┐ │
│  │ followUser   │        │ uploadProfile│      │ fetchFollow│ │
│  │ unfollowUser │        │    Picture   │      │    ers     │ │
│  │ isFollowing  │        │ deleteProfile│      │ fetchFollow│ │
│  └──────────────┘        │    Picture   │      │    ing     │ │
│                          │ uploadPhoto  │      │ fetchMutual│ │
│                          └──────────────┘      └────────────┘ │
│                                                                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FirebaseManager                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐ │
│  │  Firestore   │  │   Storage    │  │    Authentication    │ │
│  │              │  │              │  │                      │ │
│  │  • Save      │  │  • Upload    │  │  • Sign In/Out       │ │
│  │  • Fetch     │  │  • Delete    │  │  • Current User      │ │
│  │  • Update    │  │  • Get URL   │  │  • Auth State        │ │
│  │  • Delete    │  │              │  │                      │ │
│  └──────────────┘  └──────────────┘  └──────────────────────┘ │
│                                                                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Firebase Backend                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Firestore Database         Firebase Storage      Auth Service │
│  ┌──────────────────┐      ┌──────────────┐      ┌──────────┐ │
│  │ • users/         │      │ profile_     │      │ Users    │ │
│  │ • follows/       │      │   images/    │      │ Sessions │ │
│  │ • notifications/ │      │ user_photos/ │      │ Tokens   │ │
│  │ • posts/         │      │ post_media/  │      │          │ │
│  └──────────────────┘      └──────────────┘      └──────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### Follow User Flow

```
User taps           FollowButton          SocialService        Firebase
"Follow"            sends action          processes            updates
   │                      │                    │                  │
   │                      │                    │                  │
   ├─────────────────────▶│                    │                  │
   │                      │                    │                  │
   │                      ├───────────────────▶│                  │
   │                      │  followUser(id)    │                  │
   │                      │                    │                  │
   │                      │                    ├─────────────────▶│
   │                      │                    │  Batch Write:    │
   │                      │                    │  1. Create       │
   │                      │                    │     relationship │
   │                      │                    │  2. Increment    │
   │                      │                    │     counts       │
   │                      │                    │  3. Notification │
   │                      │                    │◀─────────────────┤
   │                      │                    │   Success        │
   │                      │◀───────────────────┤                  │
   │                      │   Complete         │                  │
   │◀─────────────────────┤                    │                  │
   │  UI Updates          │                    │                  │
   │  (Animation)         │                    │                  │
```

### Profile Picture Upload Flow

```
User selects        ProfilePicker         SocialService        Firebase
image               processes             uploads              stores
   │                      │                    │                  │
   │                      │                    │                  │
   ├─────────────────────▶│                    │                  │
   │  Pick Image          │                    │                  │
   │                      │                    │                  │
   │                      ├────────────────┐   │                  │
   │                      │ Compress       │   │                  │
   │                      │ Preview        │   │                  │
   │                      │◀───────────────┘   │                  │
   │                      │                    │                  │
   │  Tap "Upload"        │                    │                  │
   ├─────────────────────▶│                    │                  │
   │                      │                    │                  │
   │                      ├───────────────────▶│                  │
   │                      │  upload(image)     │                  │
   │                      │                    │                  │
   │                      │                    ├─────────────────▶│
   │                      │                    │  Upload to       │
   │                      │                    │  Storage         │
   │                      │                    │◀─────────────────┤
   │                      │                    │  URL             │
   │                      │                    │                  │
   │                      │                    ├─────────────────▶│
   │                      │                    │  Update user     │
   │                      │                    │  profile         │
   │                      │                    │◀─────────────────┤
   │                      │◀───────────────────┤                  │
   │◀─────────────────────┤  URL               │                  │
   │  Dismiss, refresh    │                    │                  │
```

## 🏗️ Component Relationships

```
SocialProfileExampleView
    │
    ├─── UserService (user data)
    │       └─── FirebaseManager
    │
    ├─── SocialService (follow/photos)
    │       └─── FirebaseManager
    │
    ├─── ProfilePicturePicker (sheet)
    │       └─── SocialService
    │
    ├─── FollowersListView (sheet)
    │       ├─── SocialService
    │       └─── UserRowView
    │               └─── FollowButton
    │                       └─── SocialService
    │
    └─── FollowButton (inline)
            └─── SocialService
```

## 📁 File Organization

```
AMENAPP/
├── Models/
│   ├── UserModel.swift
│   └── FollowRelationship.swift          ← NEW
│
├── Services/
│   ├── FirebaseManager.swift             ← UPDATED
│   ├── UserService.swift
│   └── SocialService.swift               ← NEW
│
├── Views/
│   ├── Components/
│   │   ├── FollowButton.swift            ← NEW
│   │   └── ProfilePicturePicker.swift    ← NEW
│   │
│   ├── Social/
│   │   ├── FollowersListView.swift       ← NEW
│   │   └── SocialProfileExampleView.swift ← NEW
│   │
│   └── Connect/
│       └── AmenConnectView.swift         ← FIXED
│
├── Config/
│   ├── firestore.rules                   ← NEW
│   └── storage.rules                     ← NEW
│
└── Documentation/
    ├── SOCIAL_FEATURES_GUIDE.md          ← NEW
    ├── QUICK_START_SOCIAL.md             ← NEW
    └── IMPLEMENTATION_SUMMARY_SOCIAL.md  ← NEW
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        Client (Swift)                           │
│                                                                 │
│  All requests include:                                          │
│  • Firebase Auth Token (JWT)                                    │
│  • User ID from Auth                                            │
│                                                                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Firestore Rules Engine                       │
│                                                                 │
│  Validates:                                                     │
│  ✓ User is authenticated                                        │
│  ✓ User owns the resource                                       │
│  ✓ Follow relationships are valid                               │
│  ✓ Counts aren't directly manipulated                           │
│                                                                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Storage Rules Engine                         │
│                                                                 │
│  Validates:                                                     │
│  ✓ User is authenticated                                        │
│  ✓ File is correct type (image/video)                           │
│  ✓ File size is within limits                                   │
│  ✓ User uploading to own folder                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 State Management

```
┌────────────────────────────────────────────────────────────┐
│              Published Properties (Observable)             │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  SocialService                  UserService                │
│  ┌─────────────────────┐       ┌─────────────────────┐   │
│  │ @Published          │       │ @Published          │   │
│  │ • followers         │       │ • currentUser       │   │
│  │ • following         │       │ • isLoading         │   │
│  │ • isLoading         │       │ • error             │   │
│  │ • error             │       │                     │   │
│  └─────────────────────┘       └─────────────────────┘   │
│                                                            │
│  Auto-updates UI when:                                     │
│  • Follow/unfollow completes                               │
│  • Lists are fetched                                       │
│  • Profile picture is uploaded                             │
│  • User data is refreshed                                  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

## 🌊 Async Flow Pattern

```swift
// View Layer (UI)
Button("Follow") {
    Task {                              // 1. Start async context
        do {
            try await service           // 2. Call async service
                .followUser(id)
                                       // 3. UI auto-updates via @Published
        } catch {
            showError = true           // 4. Handle errors
        }
    }
}

// Service Layer
@MainActor                             // 1. Ensure main thread
class SocialService {
    @Published var followers = []       // 2. Observable state
    
    func followUser() async throws {
        defer { isLoading = false }    // 3. Always cleanup
        isLoading = true
        
        try await firebase.batch()     // 4. Network call
        
        followers = fetchUpdated()     // 5. Update triggers UI
    }
}
```

## 🎯 Optimization Points

```
┌─────────────────────────────────────────────────────────────┐
│                      Optimizations                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Database                  Storage                 Client   │
│  ┌──────────────┐         ┌──────────────┐        ┌──────┐ │
│  │ • Batch      │         │ • Compress   │        │ Cache│ │
│  │   writes     │         │   images     │        │ images│ │
│  │ • Indexes    │         │ • Size       │        │ Lazy │ │
│  │ • Denormal   │         │   limits     │        │ load │ │
│  │   counts     │         │              │        │      │ │
│  └──────────────┘         └──────────────┘        └──────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

**This architecture provides:**
- ✅ Separation of concerns
- ✅ Scalable structure
- ✅ Type-safe operations
- ✅ Error handling
- ✅ Loading states
- ✅ Security
- ✅ Performance
