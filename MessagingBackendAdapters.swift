import Foundation
import UIKit

// This extension provides adapter methods that are already implemented in FirebaseMessagingService
// but may have been called with different signatures elsewhere in the codebase.
// Methods that already exist with proper implementations have been removed from this file.

extension FirebaseMessagingService {
    // Adapter methods removed as they are implemented elsewhere.
}
