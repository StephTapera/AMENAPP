# AMEN — i18n Locale Audit Worklist (report only — do NOT apply yet)

> Generated 2026-06-20. READ-ONLY audit across 362 candidate Swift files (files using DateFormatter/NumberFormatter/String(format:)/.dateFormat).
> Applying these is a POST-QUIESCE / POST-MIGRATION step — the 366-file blast radius collides with live agents, and plural/format fixes that need String Catalog plural variants depend on the Localizable.strings → String Catalog migration happening first.
> Localization is NOT a v1 submission gate; this is a parallel track.

## Summary
- Total findings: 382
- By category: formatting 203, rtl 69, plurals 109, layout 1
- By severity: high 34, medium 199, low 149
- Top hotspot files (most findings): AMENAPP/EventsView.swift — 7; AMENAPP/CreatorView.swift — 7; AMENAPP/CreatorSubscriptionGateView.swift — 6; AMENAPP/AMENAPP/DiscoveryOS/DiscoveryPreviewSheet.swift — 6; AMENAPP/ConversationThreadView.swift — 6; AMENAPP/AMENAPP/AMENAPP/Pulse/AmenPulseSurfaceView.swift — 5; AMENAPP/PeopleDiscoveryView.swift — 5; AMENAPP/AMENAPP/AdaptiveComposer/Cards/AttachmentCardsC.swift — 5; AMENAPP/AMENAPP/Giving/GivingModels.swift — 5; AMENAPP/AMENAPP/SpiritualOS/Churches/AmenChurchHubView.swift — 5

## Apply ordering (recommended)
1. Date/time format fixes (`"h a"`, `"EEEE, MMMM d"`, `"MMM d, yyyy"` → locale-aware FormatStyle / dateStyle/timeStyle) — pure code, safe pre-migration.
2. Number/currency fixes (`String(format:"$%.2f")`, `"%.1f mi"`, `"%.1fK"` → .formatted(.currency/.number/.measurement)) — pure code, safe pre-migration.
3. RTL phrasing fixes — convert concatenation to localized format strings; lands cleanly once strings live in the String Catalog (post-migration).
4. Plural fixes — REQUIRE String Catalog/.stringsdict plural variants; do AFTER migration.

## 1. Formatting (date / number / currency)
| File:Line | Severity | Issue | Proposed fix |
|---|---|---|---|
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCovenantTierSetupSheet.swift:168 | high | formattedPrice() builds a displayed price by hardcoding "$" for USD and otherwise concatenating the currency code, then String(format: "%.2f") for the amount. The amount uses no locale-aware decimal/grouping separator,… | Use tier.price.formatted(.currency(code: tier.currency)) which produces the correct symbol, placement, and locale-aware separators, then append the billing period via a localized format string with a positional placehol… |
| AMENAPP/AMENAPP/AdaptiveComposer/Cards/AttachmentCardsC.swift:70 | high | Donation 'raised' amount is built by manually prepending payload.currency and casting the amount to Int: "\(payload.currency)\(Int(payload.raisedAmount)) raised". This drops fractional value and grouping separators, ass… | Use a currency NumberFormatter (numberStyle = .currency, currencyCode = payload.currency, locale = .autoupdatingCurrent) or payload.raisedAmount.formatted(.currency(code: payload.currency)), then place it via a localize… |
| AMENAPP/AMENAPP/AdaptiveComposer/Cards/AttachmentCardsC.swift:74 | high | Donation goal amount built the same way: "Goal: \(payload.currency)\(Int(payload.goalAmount))" — manual symbol placement, Int truncation, no grouping/locale awareness. | Format payload.goalAmount with a locale-aware currency formatter (currencyCode = payload.currency) and use a localized "Goal: %@" format string. |
| AMENAPP/AMENAPP/ChurchJourneyPlanner.swift:103 | high | departureSummary uses a hardcoded literal dateFormat "h:mm a" for displayed departure/service times. This forces a 12-hour AM/PM clock with a hardcoded English AM/PM symbol; locales that use a 24-hour clock (most of Eur… | Use a locale-aware time style: formatter.timeStyle = .short; formatter.dateStyle = .none (formatter.locale defaults to .current), or replace with departure.formatted(.dateTime.hour().minute()). |
| AMENAPP/AMENAPP/CommunityOS/Monetization/AmenPlanComparisonView.swift:237 | high | Price is rendered by hardcoding the '$' symbol and formatting the number with String(format: "%.0f"), so it always shows USD with a US grouping/decimal convention regardless of the user's locale or currency. | Use a currency-aware format, e.g. Text(priceUSD.formatted(.currency(code: "USD").precision(.fractionLength(0)))) or a NumberFormatter with .numberStyle = .currency and .locale = .current, so the symbol placement and dig… |
| AMENAPP/AMENAPP/ConnectSpaces/Events/AmenBroadcastComposerView.swift:363 | high | Date.shortLabel uses literal DateFormatter formats with hardcoded English words ('Today','Tomorrow','at') and a 12-hour h:mma pattern ("EEE, MMM d 'at' h:mma"), which is untranslated and ignores 24-hour/locale date orde… | Use Date.FormatStyle / locale templates for the date and time portions and localize the 'Today'/'Tomorrow'/'at' connectors; do not embed literal English words in dateFormat. |
| AMENAPP/AMENAPP/ConnectSpaces/Events/AmenSpaceEventDetailView.swift:123 | high | Event date/time displayed via a custom DateFormatter with a literal .dateFormat pattern "EEEE, MMMM d 'at' h:mm a" (see private Date.formatted(as:) at line 474-479). Hardcoded English month/weekday ordering, the literal… | Use Date.FormatStyle: event.scheduledAt.formatted(.dateTime.weekday(.wide).month(.wide).day().hour().minute()) which localizes order, month/weekday names, and 12/24-hour clock automatically. |
| AMENAPP/AMENAPP/ConnectSpaces/Events/AmenSpaceEventsListView.swift:148 | high | Date.shortLabel builds displayed event dates/times with literal DateFormatter formats ("'Today' h:mma", "'Tomorrow' h:mma", "MMM d, h:mma"). The literal 'Today'/'Tomorrow' words are untranslated and the h:mma pattern fo… | Use locale-aware formatting: for the time, scheduledAt.formatted(.dateTime.hour().minute()); for today/tomorrow use RelativeDateTimeFormatter or a localized template (Date.FormatStyle with .day()/.month()), and pull 'To… |
| AMENAPP/AMENAPP/ConnectSpaces/Legal/AmenSpaceLegalGateView.swift:45 | high | formattedPrice uses "$\(Int(dollars))/mo" and String(format: "$%.2f/mo", dollars), hardcoding the dollar symbol and decimal/suffix for the price shown in the legal join gate header. | Format the amount via a currency NumberFormatter / Decimal.formatted(.currency(code:)) and localize the "/mo" suffix. |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenCreatorEarningsDashboard.swift:695 | high | centsToString hardcodes the dollar sign via String(format: "$%,.0f"/"$%,.2f", ...). The %,.f grouping uses the C-locale and the $ is fixed — wrong symbol, grouping and decimal separators outside US locales. This drives… | Use a currency NumberFormatter (numberStyle = .currency, locale = .autoupdatingCurrent or the merchant currency) or Decimal(cents)/100 .formatted(.currency(code: "USD")). |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenGiftMembershipView.swift:85 | high | giftPriceString uses String(format: "$%.2f", dollars), hardcoding the US dollar symbol and decimal convention for a prominent displayed price (and the CTA/summary that interpolate it). | Use a currency NumberFormatter or Decimal.formatted(.currency(code:)) so symbol, placement and separators are locale-correct. |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenGiftMembershipView.swift:488 | high | GiftTierCard monthly price uses "$\(Int(dollars))/mo" and String(format: "$%.2f/mo", dollars) — hardcoded $ symbol, decimal point, and a fixed "/mo" suffix with manual ordering. | Format the amount with a currency formatter and move the "per month" suffix into a localized format string with a positional placeholder. |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenSpacePaywallView.swift:236 | high | Monthly price displayed with String(format: "$%.2f", dollars) and manual "$\(Int(dollars))" — hardcodes the US dollar sign and decimal convention. Wrong symbol/placement/separators in other locales. | Format with a currency NumberFormatter (numberStyle = .currency, currencyCode from the product) or Decimal.formatted(.currency(code:)); never hand-build the $ symbol. |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenSpacePaywallView.swift:293 | high | Intro badge label "First \(months) mo. at \(priceStr)/mo" hand-builds the price ($ via String(format:)) and a hardcoded "mo."/"/mo" with manual ordering; not currency-locale-aware, not pluralized, and RTL-unsafe. | Build priceStr via a currency formatter and wrap the whole sentence in a localized format string with positional placeholders and a plural-aware month count. |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenSpaceTierSelectionView.swift:159 | high | priceLabel uses "$\(Int(dollars))/mo" and String(format: "$%.2f/mo", dollars) — hardcoded dollar sign, decimal convention and "/mo" suffix for a displayed price. | Use a currency NumberFormatter for the amount and a localized format string for the "/mo" suffix. |
| AMENAPP/AMENAPP/Giving/GivingModels.swift:368 | high | requestedAmountFormatted produces a displayed amount by manual integer division and literal "$" prefix: "$\(dollars)". Not locale-aware (symbol, placement, grouping) and ignores the model's own currency field. | Use Decimal(requestedAmount)/100 formatted with .currency(code: currency) so the symbol/placement/separators follow locale and the actual currency code. |
| AMENAPP/AMENAPP/Giving/GivingModels.swift:491 | high | amountFormatted uses String(format: "$%.2f", dollars): hardcoded "$" symbol and period decimal separator. Wrong symbol/placement/separator for non-US locales; this is a tax receipt amount shown to the user. | Use (Decimal(amount)/100).formatted(.currency(code: currency)) for a locale- and currency-correct string. |
| AMENAPP/ChurchCapsuleView.swift:18 | high | Displayed service date is built with a hardcoded DateFormatter.dateFormat of "EEE MMM d". This English-pattern format is not locale-aware: it forces English-ordered/abbreviated weekday+month and ignores the user's local… | Use a locale-aware skeleton or FormatStyle, e.g. date.formatted(.dateTime.weekday(.abbreviated).month(.abbreviated).day()), or set formatter.setLocalizedDateFormatFromTemplate("EEEMMMd") with formatter.locale = .autoupd… |
| AMENAPP/CreatorSubscriptionGateView.swift:147 | high | Subscription price is built with a hardcoded dollar sign and US grouping via String(format: "$%.2f", ...). In non-US/non-USD locales this shows the wrong currency symbol, wrong decimal separator, and wrong symbol placem… | Format with a currency NumberFormatter or .formatted: e.g. `subscriptionPrice.formatted(.currency(code: currencyCode))` where currencyCode comes from the product/StoreKit, or at minimum a NumberFormatter with numberStyl… |
| AMENAPP/CreatorSubscriptionGateView.swift:281 | high | Price row builds currency with a hardcoded "$" prefix and String(format: "%.2f", ...): "$\(String(format: \"%.2f\", creator.subscriptionPrice)) / mo". Wrong symbol/separators in non-USD locales. | Use creator.subscriptionPrice.formatted(.currency(code: creator.currencyCode)) or a currency NumberFormatter with locale = .autoupdatingCurrent; localize the " / mo" suffix separately. |
| AMENAPP/CreatorView.swift:1111 | high | Subscription price displayed as "$\(String(format: "%.2f", price))/mo" — hardcoded currency symbol, placement and decimal format, plus an English "/mo" abbreviation. Not locale-aware for currency and not localizable for… | Format the amount with .formatted(.currency(code:)) / NumberFormatter(.currency) and build the per-month string from a localized format string with a positional placeholder, e.g. String(format: NSLocalizedString("%@/mo"… |
| AMENAPP/CreatorView.swift:1301 | high | Tip amount button label built as "$\(Int(amt))" — hardcoded leading dollar sign and symbol placement. Wrong for locales whose currency symbol trails or differs. | Use Double(amt).formatted(.currency(code: "USD").precision(.fractionLength(0))) or a NumberFormatter with .currency style and .current locale. |
| AMENAPP/CreatorView.swift:1350 | high | Send button label "Send $\(Int(selectedAmount)) Gift" hardcodes the "$" symbol and its placement inline in a sentence, which also assumes English word order. Not locale-aware for currency and not localizable as a senten… | Format the amount via .formatted(.currency(code:)) and compose the sentence with a localized format string with a positional placeholder, e.g. String(format: NSLocalizedString("Send %@ Gift", ...), formattedAmount). |
| AMENAPP/CreatorViewModel.swift:18 | high | Monthly revenue is displayed by concatenating a literal "$" with String(format: "%.0f"). The currency symbol, symbol placement, grouping separators, and decimal handling are hardcoded to a US/en convention. In locales w… | Use a locale-aware currency formatter, e.g. profile.monthlyRevenue.formatted(.currency(code: "USD").precision(.fractionLength(0))) or NumberFormatter with numberStyle = .currency and locale = .current. Same fix applies… |
| AMENAPP/FirstVisitCompanionView.swift:168 | high | Weekday header initials are hardcoded English (["S","M","T","W","T","F","S"]) and the grid is hardcoded Sunday-first (firstWeekday = weekday - 1 at line 271), so the displayed calendar shows wrong weekday letters and wr… | Derive symbols from Calendar.current.veryShortWeekdaySymbols reordered by Calendar.current.firstWeekday, and compute the leading-pad offset relative to firstWeekday instead of assuming Sunday=0. |
| AMENAPP/MentorModel.swift:45 | high | priceLabel builds currency with a hardcoded '$' and a manually appended '/mo' suffix via String(format:), which is not locale- or currency-aware and renders wrong for non-USD/non-US locales. | Format the amount with NumberFormatter (.numberStyle = .currency, .locale = .autoupdatingCurrent) or priceMonthly.formatted(.currency(code:)), and move the '/mo' into a localized format string with a positional placehol… |
| AMENAPP/PostSchedulingSystem.swift:280 | high | Scheduled-post label uses DateFormatter literal .dateFormat = "EEE, MMM d \u{00B7} h:mm a" for user-facing 'Schedule for ...' CTA and row labels. Hardcoded English weekday/month abbreviations and 12-hour h:mm a clock; w… | Use self.formatted(.dateTime.weekday(.abbreviated).month(.abbreviated).day().hour().minute()); keep the '\u{00B7}' separator only as decoration, or rely on the style's localized layout. |
| AMENAPP/ScheduledMessage.swift:68 | high | scheduledAtFormatted uses hardcoded DateFormatter.dateFormat strings with embedded English literals ('Today at', 'Tomorrow at', 'at') and a 12-hour 'h:mm a' assumption for DISPLAYED text. In non-English/24-hour/RTL loca… | Use a locale-aware Date.FormatStyle for the time (e.g. scheduledAt.formatted(date: .omitted, time: .shortened)) and provide the 'Today at {time}'/'Tomorrow at {time}'/'{date} at {time}' wrapper via localized String(form… |
| AMENAPP/TipSheetView.swift:155 | high | Preset amount buttons use String(format:"$%.0f") — hardcoded currency symbol and format, user-facing. | amount.formatted(.currency(code: "USD").precision(.fractionLength(0))). |
| AMENAPP/TipSheetView.swift:219 | high | Send button label concatenates a hardcoded '$' amount via String(format:"$%.0f"). User-facing currency not locale-aware. | Build the amount with effectiveAmount.formatted(.currency(code:"USD")) and a localized format string for the surrounding 'Send with …' text. |
| AMENAPP/TipSheetView.swift:283 | high | Success confirmation 'You sent $5 to Name' built with String(format:"You sent $%.0f to %@") — hardcoded '$', non-locale currency, and fixed word order (also an RTL concern). | Use a localized format string with the currency formatted via .formatted(.currency(code:"USD")) and positional placeholders, e.g. NSLocalizedString("tip.sent.confirmation") with %@ for amount and %@ for name. |
| AMENAPP/TipView.swift:273 | high | Currency built with hardcoded '$' and String(format:"$%.0f"). This is user-facing money shown in 'Send $5' and 'You sent $5 to X'. Not locale-aware: wrong currency symbol/placement/grouping for non-US locales. | Use a currency NumberFormatter or FormatStyle: effectiveAmount.formatted(.currency(code: "USD")) (or the app's actual charge currency). This places the symbol and separators per .current locale instead of a hardcoded le… |
| AMENAPP/TipView.swift:288 | high | Amount preset cards render currency with hardcoded '$' via String(format:"$%.0f"). User-facing tip amounts ($1/$3/$5/$10) are not locale-aware. | amount.formatted(.currency(code: "USD").precision(.fractionLength(0))) so symbol/placement follow locale. |
| AMENAPP/AIBibleStudyView.swift:1522 | medium | Devotional header date is displayed via DateFormatter with a hardcoded literal dateFormat 'MMMM d, yyyy', forcing US month-day-year ordering for all locales. | Use Date().formatted(.dateTime.month(.wide).day().year()) or DateFormatter with dateStyle = .long (locale .current) so month/day/year ordering localizes. |
| AMENAPP/AIDailyVerseView.swift:71 | medium | The displayed month abbreviation is produced with a fixed DateFormatter().dateFormat = "MMM". While "MMM" is a symbolic format, building it without setting a locale and then uppercasing assumes Latin-script month abbrev… | Use Date.now.formatted(.dateTime.month(.abbreviated)) (locale-aware) and drop the manual .uppercased(); if a compact style is needed, set formatter.locale = .autoupdatingCurrent and let the locale decide casing. |
| AMENAPP/AMENAPP/AIIntelligence/BereanPersonalContextProvider.swift:185 | medium | humanLabel(for:prefix:) builds user-facing AI context labels (e.g. "your note from June 14") with a hardcoded DateFormatter.dateFormat "MMMM d", which hardcodes English month-name-before-day ordering. This text is surfa… | Use `formatter.setLocalizedDateFormatFromTemplate("MMMMd")` or `date.formatted(.dateTime.month(.wide).day())`. Also consider that the surrounding "\(prefix) \(date)" concatenation is not RTL/translation-safe (see rtl fi… |
| AMENAPP/AMENAPP/AMENAPP/BereanPulse/BereanPulseView.swift:434 | medium | The header title date is rendered with a literal DateFormatter.dateFormat = "MMMM d", which hardcodes English month-then-day ordering and ignores locale conventions (e.g. day-first or non-Latin months). | Use Date.FormatStyle, e.g. Date().formatted(.dateTime.month(.wide).day()), which respects the user's locale ordering and month names. |
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCovenantDiscoveryView.swift:350 | medium | joinLabel hardcodes currencyCode = "USD" and never sets a locale, so the membership starting price is always shown as US dollars regardless of the user's region or the creator's actual currency. | Drive currencyCode from the tier's actual currency (as AmenCreatorHubView does) and set `fmt.locale = .autoupdatingCurrent`. Avoid hardcoding USD for a global audience. |
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCovenantEventsView.swift:372 | medium | Displayed event date/time is built with a hardcoded DateFormatter.dateFormat string "EEEE, MMM d · h:mm a". This forces US English ordering and a 12-hour AM/PM clock regardless of the user's locale; in locales that use… | Use a locale-aware style: `date.formatted(.dateTime.weekday(.wide).month().day().hour().minute())`, or set `formatter.setLocalizedDateFormatFromTemplate("EEEEMMMdjmm")` so the time field follows the locale's 12/24-hour… |
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCovenantRoomDetailView.swift:144 | medium | Day-group separator label built with DateFormatter literal .dateFormat = "MMM d" for displayed text. 'Today'/'Yesterday' are correctly localized via Calendar, but the fallback month/day uses a fixed English-centric patt… | Replace with date.formatted(.dateTime.month(.abbreviated).day()) for locale-correct month abbreviation and day/month ordering. (Note: 'Today'/'Yesterday' string literals at lines 141-142 should also be NSLocalizedString… |
| AMENAPP/AMENAPP/AMENAPP/ObjectHub/AmenMentorChannelView.swift:1261 | medium | OfficeHourCard date label uses hardcoded DateFormatter.dateFormat "EEE MMM d" for displayed text, which hardcodes English-style month-before-day ordering. | Use `f.setLocalizedDateFormatFromTemplate("EEEMMMd")` or `date.formatted(.dateTime.weekday(.abbreviated).month().day())` so field ordering follows the user's locale. |
| AMENAPP/AMENAPP/AMENAPP/ObjectHub/AmenObjectDiscussionRoomView.swift:26 | medium | Date-separator label built with hardcoded literal dateFormat "MMM d" (groupedMessages). The label is user-visible chat date grouping; fixed month-then-day ordering and non-localized abbreviation render incorrectly in ma… | Use f.setLocalizedDateFormatFromTemplate("MMMd"), or replace with date.formatted(.dateTime.month(.abbreviated).day()). ("Today"/"Yesterday" strings should also be localized via String Catalog.) |
| AMENAPP/AMENAPP/AMENAPP/Pulse/AmenPulseSurfaceView.swift:434 | medium | Displayed date built with a hardcoded DateFormatter.dateFormat literal "EEEE, MMMM d". This fixes the field order (weekday, month, day) and omits locale-aware ordering/format; in other locales the day/month order and se… | Use Date.FormatStyle: Date().formatted(.dateTime.weekday(.wide).month(.wide).day()) (or DateFormatter with setLocalizedDateFormatFromTemplate("EEEEMMMMd")) so the field order follows the user's locale. |
| AMENAPP/AMENAPP/AMENAPP/Pulse/AmenPulseSurfaceView.swift:556 | medium | dateEyebrow() uses the same hardcoded display date format "EEEE, MMMM d" for a user-visible eyebrow label; not locale-aware field ordering. | Replace with Date().formatted(.dateTime.weekday(.wide).month(.wide).day()) or a template-based localized format so non-English locales render the correct order. |
| AMENAPP/AMENAPP/ChurchJourneyModels.swift:140 | medium | Displayed service time uses a hardcoded literal pattern: formatter.dateFormat = "EEEE, MMM d • h:mm a". This forces English-style ordering, the 12-hour clock with AM/PM, and the comma/space layout regardless of locale (… | Use serviceStartAt.formatted(.dateTime.weekday(.wide).month(.abbreviated).day().hour().minute()) or a DateFormatter with dateStyle/timeStyle (+ locale = .autoupdatingCurrent) so the clock format and field order follow t… |
| AMENAPP/AMENAPP/CommunityOS/Church/AmenChurchDiscoveryView.swift:340 | medium | distanceLabel formats displayed distance with String(format: "%.1f mi"/"%.0f mi", ...) plus a literal '< 0.1 mi', hardcoding the decimal separator and forcing miles regardless of locale. | Format via Measurement(value: d, unit: UnitLength.miles).formatted(.measurement(width: .abbreviated, usage: .road)) so the separator and unit follow the user's locale. |
| AMENAPP/AMENAPP/CommunityOS/Church/AmenChurchPassportView.swift:256 | medium | Visit date for display is built with DateFormatter().dateFormat = "MMM d, yyyy", a hardcoded US-style pattern; other locales order/format dates differently. | Use stamp.visitDate.formatted(.dateTime.month(.abbreviated).day().year()) or DateFormatter with dateStyle = .medium so the locale controls field ordering. |
| AMENAPP/AMENAPP/CommunityOS/Church/ChurchObjectHub.swift:539 | medium | Month abbreviation badge built with DateFormatter().dateFormat = "MMM" for displayed text; bypasses locale-aware month naming and is later force-.uppercased() which is wrong in many locales. | Use event.startDate.formatted(.dateTime.month(.abbreviated)) and drop the manual .uppercased() (let the locale decide casing). |
| AMENAPP/AMENAPP/CommunityOS/Content/ContentObjectCardView.swift:295 | medium | formattedCount builds visible stat counts with String(format: "%.0fK"/"%.1fK"/"%.1fM"), hardcoding the '.' decimal separator and non-localized digits. | Format the number with a locale-aware NumberFormatter (.locale = .current) / value.formatted() and append the localized K/M unit so the decimal separator follows the locale. |
| AMENAPP/AMENAPP/CommunityOS/Discussion/AmenDiscussionRoomListView.swift:483 | medium | relativeTime renders displayed timestamps with concatenated '\(Int(...))m'/'h' units and a hardcoded DateFormatter pattern "MMM d", neither locale-aware. | Use RelativeDateTimeFormatter for relative durations and date.formatted(.dateTime.month(.abbreviated).day()) for the absolute fallback. |
| AMENAPP/AMENAPP/CommunityOS/Discussion/AmenDiscussionThreadView.swift:629 | medium | relativeTime renders displayed timestamps with concatenated '\(Int(...))m'/'h' units and a hardcoded DateFormatter pattern "MMM d"; none are locale-aware (units and date field order). | Use RelativeDateTimeFormatter (or Date.RelativeFormatStyle) for the m/h portions and date.formatted(.dateTime.month(.abbreviated).day()) for the fallback. |
| AMENAPP/AMENAPP/CommunityOS/Emergence/CommunityEmergenceBannerView.swift:24 | medium | Member count abbreviation builds '1.2K'/'1.1M' via String(format:"%.1fK")/'%.1fM' with no locale. The decimal separator is wrong in locales that use a comma (e.g. '1,2 K' in fr/de) and the K/M suffixes are not localized… | Use a locale-aware compact formatter, e.g. Int(self).formatted(.number.notation(.compactName)) (which yields localized '1.2K'/'1,2K'/'1.2万'), then append the localized 'members' plural separately. |
| AMENAPP/AMENAPP/CommunityOS/HeroExperience/CommunitySnapshotView.swift:96 | medium | communityAbbreviated builds displayed counts with String(format: "%.1fM"/"%.1fK") and "\(self)", using a hardcoded '.' decimal separator and non-localized digits for user-visible metric chips. | Format the numeric portion with a locale-aware NumberFormatter (.locale = .current) or value.formatted(...) and append the localized K/M unit, so the decimal separator and grouping follow the user's locale. |
| AMENAPP/AMENAPP/CommunityOS/HeroExperience/ContentHeroViewModel.swift:182 | medium | communityFormatted builds displayed counts with String(format: "%.1fM"/"%.1fK"), hardcoding the decimal separator and non-localized digits for user-visible summary text. | Format the numeric portion with a locale-aware NumberFormatter (.locale = .current) or value.formatted(...) before appending the K/M unit. |
| AMENAPP/AMENAPP/CommunityOS/Monetization/AmenFeatureGateView.swift:122 | medium | The monthly price is rendered by manually concatenating a hardcoded "$" prefix and "/month" suffix around String(format: "%.0f", priceUSD). The currency symbol, symbol placement, grouping/decimal separators, and the "/m… | Format the amount with a currency-aware NumberFormatter (numberStyle = .currency, currencyCode = "USD", locale = .autoupdatingCurrent) or Decimal(priceUSD).formatted(.currency(code: "USD")), then place it in a localized… |
| AMENAPP/AMENAPP/CommunityOS/Prayer/AmenPrayerFeedView.swift:438 | medium | The fallback displayed date in `timeAgo` uses a hardcoded DateFormatter pattern "MMM d", which renders English month names regardless of the user's locale (e.g. Arabic/Japanese users still see "Jun 5"). | Replace the literal pattern with a locale-aware style: `date.formatted(.dateTime.month(.abbreviated).day())`, or a DateFormatter using `setLocalizedDateFormatFromTemplate("MMMd")`. Better still, use `RelativeDateTimeFor… |
| AMENAPP/AMENAPP/CommunityOS/Prayer/PrayerRoomView.swift:69 | medium | Prayer-update timestamp falls back to a DateFormatter with literal .dateFormat = "MMM d" for displayed text. Abbreviated month name and month-before-day order are English-centric, not locale-aware. | Use createdAt.formatted(.dateTime.month(.abbreviated).day()) so the month abbreviation is localized and day/month order follows locale. |
| AMENAPP/AMENAPP/ConnectSpaces/AmenSpaceDiscoveryView.swift:262 | medium | Abbreviated count built with String(format: "%.1fK members", ...) — the "K" abbreviation and the decimal separator are hardcoded and not locale-aware (e.g. "1.5K" vs locales using comma decimals or different magnitude a… | Use NumberFormatter with numberStyle = .decimal (or a notation/compact-style formatter) with locale = .autoupdatingCurrent for the number, and supply "members" via a localized plural string. |
| AMENAPP/AMENAPP/ConnectSpaces/Events/AmenEventBroadcastView.swift:585 | medium | broadcastShortLabel embeds literal English words inside the DateFormatter pattern ("'Today at' h:mm a", "'Tomorrow at' h:mm a", "EEE, MMM d 'at' h:mm a"), so the displayed event time is always English and 12-hour even i… | Use a localized RelativeDateTimeFormatter or a localized format string with a Date.FormatStyle for the time portion, and pull "Today"/"Tomorrow" from localized strings rather than baking them into the format pattern. |
| AMENAPP/AMENAPP/ConnectSpaces/Events/AmenSmartEventComposerView.swift:135 | medium | The shared/displayed invite text builds the event date with a hardcoded DateFormatter pattern "EEEE, MMMM d 'at' h:mm a", which forces English month/weekday names, US comma ordering, and 12-hour AM/PM regardless of the… | Use selectedDate.formatted(.dateTime.weekday(.wide).month(.wide).day().hour().minute()) or a Date.FormatStyle so the day/time render in the device locale (e.g. 24-hour clock, localized month names). |
| AMENAPP/AMENAPP/ConnectSpaces/Events/AmenUpcomingLiveChipView.swift:22 | medium | Countdown fallback uses literal DateFormatter formats with a hardcoded 'Tomorrow' word and 12-hour h:mma pattern ("'Tomorrow' h:mma" / "EEE h:mma"), untranslated and not 24-hour/locale aware. | Format the time via scheduledAt.formatted(.dateTime.hour().minute()) and weekday via .weekday(.abbreviated), and pull 'Tomorrow' from a localized string rather than embedding it in dateFormat. |
| AMENAPP/AMENAPP/ConnectSpaces/Spaces/AmenMinistryRoomDecisionsTab.swift:41 | medium | decisionDateFormatter uses a hardcoded literal dateFormat "MMM d" for the displayed "Made <date>" text (lines 94, 98, 123). Fixed month-then-day ordering is wrong for locales that order day-before-month, and the abbrevi… | Use f.setLocalizedDateFormatFromTemplate("MMMd") so the field order follows the user's locale, or replace with item.createdAt.formatted(.dateTime.month(.abbreviated).day()). |
| AMENAPP/AMENAPP/Covenant/AmenCovenantPaywallView.swift:15 | medium | The shared currency NumberFormatter sets .numberStyle = .currency and currencyCode but never sets .locale, so it falls back to a default locale for grouping/decimal separators and symbol placement instead of the user's… | Set formatter.locale = .autoupdatingCurrent (in addition to currencyCode) so separators and symbol placement follow the user's locale while keeping the tier's currency code. |
| AMENAPP/AMENAPP/Covenant/AmenCovenantRevenueView.swift:18 | medium | The shared currencyFormatter sets currencyCode = "USD" and never sets .locale. Revenue (MRR) is displayed to the creator via formatCurrency(). The currency symbol and grouping/decimal separators will be wrong for non-US… | Set f.locale = Locale.autoupdatingCurrent so grouping/decimal separators localize. If the value is truly USD-denominated keep currencyCode = "USD"; otherwise drop the hardcoded currencyCode and use the locale's currency… |
| AMENAPP/AMENAPP/DiscoveryOS/DiscoveryCardView.swift:222 | medium | Distance label String(format: "%.1f km away", km) hardcodes "km" unit and decimal format, ignoring user locale (miles vs km, decimal separator). | Use Measurement<UnitLength> with .formatted(.measurement(...)) and a localized "%@ away" wrapper, honoring locale. |
| AMENAPP/AMENAPP/DiscoveryOS/DiscoveryPreviewSheet.swift:180 | medium | Distance shown as String(format: "%.1f km away", dist / 1000) — hardcodes the "km" unit and decimal format, ignoring locale (US users expect miles) and locale decimal separators. | Use Measurement<UnitLength>(value: dist, unit: .meters).formatted(.measurement(width: .abbreviated)) with the user's locale, and a localized "%@ away" wrapper string. |
| AMENAPP/AMENAPP/Features/Bridges/GivingMemory/GivingMemoryService.swift:154 | medium | GivingSummary.formattedTotal uses a currency NumberFormatter (good) but its fallback hardcodes a '$' prefix: "$\(totalAmount)". If the formatter returns nil for a non-USD currency, the donation total renders with a wron… | Set fmt.locale = .autoupdatingCurrent and make the fallback currency-neutral, e.g. return fmt.string(from: NSNumber(value: totalAmount)) ?? "\(currency) \(totalAmount)" so the user never sees a '$' for a non-USD gift. |
| AMENAPP/AMENAPP/Giving/GivingModels.swift:165 | medium | programCentsLabel builds "\(cents)¢ of every dollar to programs" with a hardcoded cents glyph and English phrasing. The "¢" symbol and "dollar" are US-currency specific and the whole string is non-localized. | Localize via a format string with a positional placeholder for the percentage/ratio (e.g. ratio.formatted(.percent)) and avoid the hardcoded ¢/dollar wording for non-USD locales. |
| AMENAPP/AMENAPP/Giving/GivingModels.swift:373 | medium | approvedCapFormatted builds "$\(cap / 100) cap" with a literal "$" and English "cap" suffix; not locale-aware and ignores the currency field. | Format the amount with .currency(code: currency) and move the " cap" suffix into a localized format string with a positional placeholder. |
| AMENAPP/AMENAPP/Giving/GivingModels.swift:526 | medium | totalFormatted builds "$\(totalGiving / 100)" with a literal "$" and integer math; not locale-aware (symbol/placement/grouping) for the annual review total shown to the user. | Format with .currency(code:) using the appropriate currency code (USD if fixed) so the symbol and grouping separators localize. |
| AMENAPP/AMENAPP/Giving/Views/TaxCenterView.swift:220 | medium | Displayed deductible total is built with a hardcoded dollar format string (String(format: "$%.2f")), so it always shows a US dollar sign and US decimal separator regardless of locale or the receipt's actual currency. | Use a locale-aware currency formatter, e.g. dollars.formatted(.currency(code: receiptCurrency)) (or NumberFormatter with .numberStyle = .currency and .locale = .current), driving the code from the receipts' currency fie… |
| AMENAPP/AMENAPP/IntegrationOS/Health/HealthWellnessView.swift:106 | medium | Displayed sleep value built with String(format: "%.1f", summary.sleepHours). The decimal separator is forced to a period; locales using a comma decimal separator will display incorrectly. | Use summary.sleepHours.formatted(.number.precision(.fractionLength(1))) for a locale-aware decimal separator. |
| AMENAPP/AMENAPP/Intelligence/ChurchPulseModels.swift:104 | medium | lastUpdatedLabel builds relative-time text with hardcoded English strings ("Updated just now", "Updated 1 hour ago", "Updated \(Int(ageHours)) hours ago") via manual math and concatenation. Not localized, hardcoded sing… | Use RelativeDateTimeFormatter (locale-aware, handles plural rules) or Date(...).formatted(.relative(presentation: .named)) to produce the relative string, wrapped with a localized "Updated %@" format. |
| AMENAPP/AMENAPP/SabbathMode/Rhythm/SabbathRhythmSettingsView.swift:190 | medium | hourLabel renders displayed schedule times (Starts/Ends pickers and the footer 'runs from <X> until <Y>') with a hardcoded DateFormatter.dateFormat = "h a". This is not locale-aware and forces 12-hour AM/PM for all user… | Use `formatter.dateStyle = .none; formatter.timeStyle = .short` (or `date.formatted(.dateTime.hour())`) so the hour respects the user's locale and 12/24-hour preference. weekdayName already uses Calendar.weekdaySymbols… |
| AMENAPP/AMENAPP/SafetySettingsView.swift:114 | medium | The trust percentage is rendered with String(format: "%.0f%%", trustScore * 100), which hardcodes the literal '%' symbol and non-localized digits; some locales position the percent sign differently and use different dig… | Use a percent format, e.g. Text(trustScore.formatted(.percent.precision(.fractionLength(0)))) or a NumberFormatter with .numberStyle = .percent and .locale = .current. |
| AMENAPP/AMENAPP/SocialGraph/Services/SmartActivityCopyGenerator.swift:114 | medium | Relative time is hand-built ("just now", "\(mins)m ago", "\(hrs)h ago", "\(days)d ago") with hardcoded English units and word order — not locale-aware and wrong in RTL. | Use RelativeDateTimeFormatter (locale = .autoupdatingCurrent) or date.formatted(.relative(presentation: .named)) to produce localized, RTL-correct relative time. Keep the medium-style fallback but it is already fine via… |
| AMENAPP/AMENAPP/SpiritualOS/Churches/AmenChurchHubView.swift:1063 | medium | Event date-badge day is built with literal DateFormatter.dateFormat = "d" and month with "MMM"; the abbreviated month is English-only and the date is not locale-aware. | Use Date.FormatStyle for displayed badge text, e.g. event.startDate.formatted(.dateTime.day()) and .month(.abbreviated), so month abbreviations localize. |
| AMENAPP/AMENAPP/SpiritualOS/DailyDigest/AmenDailyDigestView.swift:171 | medium | The date eyebrow displayed in the Amen Daily header is built with a hardcoded DateFormatter().dateFormat = "EEE, MMM d". This forces US English weekday/month ordering and abbreviations regardless of the user's locale, s… | Use a locale-aware format style instead of a literal pattern, e.g. Date().formatted(.dateTime.weekday(.abbreviated).month(.abbreviated).day()) which respects the current locale's ordering and translation. If a fixed ske… |
| AMENAPP/AMENAPP/SpiritualOS/Discovery/AmenChurchHeroCard.swift:27 | medium | distanceLabel builds displayed distance with String(format: "%.1f mi", ...): the decimal separator is hardcoded to a period and the unit is forced to miles regardless of the user's locale/measurement system. | Use Measurement(value: distanceMiles, unit: UnitLength.miles).formatted(.measurement(width: .abbreviated)) so the number formats per locale and converts to km where appropriate. |
| AMENAPP/AMENAPP/SpiritualOS/LifePlanner/AmenLifePlannerSectionView.swift:20 | medium | Displayed planner header date uses a hardcoded DateFormatter.dateFormat = "EEEE, MMM d". Weekday/month names are English and the field ordering is fixed, which is wrong for non-English locales. | Replace with a localized style: Date().formatted(.dateTime.weekday(.wide).month(.abbreviated).day()), or DateFormatter().setLocalizedDateFormatFromTemplate("EEEEMMMd") so component order and names follow the locale. |
| AMENAPP/AMENAPP/SpiritualOS/LifePlanner/AmenLifePlannerView.swift:16 | medium | monthYearFormatter uses a hardcoded literal dateFormat "MMMM yyyy" for the displayed month/year header (line 118). A fixed pattern ignores locale-specific month/year ordering and script (e.g. Japanese renders year-then-… | Use a localized template instead of a fixed pattern: f.setLocalizedDateFormatFromTemplate("MMMMyyyy") (formatter.locale defaults to .current), or replace with viewModel.selectedDate.formatted(.dateTime.month(.wide).year… |
| AMENAPP/AMENAPP/SpiritualOS/SpacesDashboard/AmenSpacesHeroCardSection.swift:140 | medium | DashboardStatRow.nextEventLabel builds displayed text with hardcoded DateFormatter.dateFormat "EEE h a", forcing English weekday abbreviation and 12-hour AM/PM clock; wrong in 24-hour locales. (Note: the accessibility l… | Use `event.startTime.formatted(.dateTime.weekday(.abbreviated).hour())` or `f.setLocalizedDateFormatFromTemplate("EEEjmm")` to respect the locale's clock convention, matching the a11y label. |
| AMENAPP/AMENAPP/SundayHomeView.swift:14 | medium | displayDateFormatter uses a hardcoded .dateFormat = "EEEE, MMMM d" and is used for the DISPLAYED date string (todayDateString, shown in the top bar), so month/weekday order and punctuation will not adapt to the user's l… | Use a locale-aware format such as Date().formatted(.dateTime.weekday(.wide).month(.wide).day()) or set the formatter's dateStyle instead of an explicit dateFormat for displayed text. |
| AMENAPP/AMENAPP/UserProfileViewMini.swift:640 | medium | formatCount builds abbreviated follower counts with String(format: "%.1fM" / "%.1fK"). The decimal separator is forced to a period and the K/M suffixes are hardcoded, so locales that use a comma decimal separator (e.g.… | Use a locale-aware compact formatter: Double(n).formatted(.number.notation(.compactName)) (iOS 15+), which produces locale-correct decimal separators and magnitude suffixes automatically. |
| AMENAPP/AMENAPP/WalkWithChristFeatures.swift:971 | medium | hourLabel builds a user-facing reminder-time string with a hardcoded DateFormatter.dateFormat = "h a". This forces a 12-hour AM/PM clock and English-style ordering for every locale; users in 24-hour locales (most of Eur… | Use a locale-aware time style instead of a fixed pattern: `let fmt = DateFormatter(); fmt.timeStyle = .short; fmt.dateStyle = .none` (which respects the user's 12/24-hour preference), or `date.formatted(.dateTime.hour()… |
| AMENAPP/AMENAuthLandingView.swift:502 | medium | formattedInlinePhoneNumber hardcodes the US country code: any number that is not already prefixed with '+' and is not an 11-digit US number gets '+1' prepended. This is shown to the user ('Sent to \(formattedInlinePhone… | Derive the default country code from the device region (e.g. Locale.current.region / a phone-number library) or require the user to pick a country code, rather than assuming +1. At minimum gate the '+1' default on a US… |
| AMENAPP/AdaptiveGlassInboxView.swift:469 | medium | Displayed greeting date is built from DateFormatter with a literal .dateFormat = "EEEE, MMMM d". This fixed English skeleton ignores locale field ordering and separators (e.g. day-before-month locales render wrong). | Use Date.now.formatted(.dateTime.weekday(.wide).month(.wide).day()) or DateFormatter.setLocalizedDateFormatFromTemplate("EEEEMMMMd") with locale = .autoupdatingCurrent so field order localizes. |
| AMENAPP/BereanAIAssistantView.swift:7949 | medium | relativeDate() falls back to a hardcoded DateFormatter.dateFormat = "MMM d" for displayed conversation dates. Abbreviated month names and day/month ordering are not locale-aware. (The "Today"/"Yesterday"/"\(days)d ago"… | Use `date.formatted(.dateTime.month(.abbreviated).day())` or a DateFormatter with `setLocalizedDateFormatFromTemplate("MMMd")` and `.locale = .autoupdatingCurrent`. |
| AMENAPP/BereanChatsListView.swift:441 | medium | Relative date fallback uses a hardcoded DateFormatter.dateFormat = "MMM d" for displayed text. Abbreviated month names won't localize and the day/month order is locale-incorrect. ("Today"/"Yesterday" branches are also h… | Use `self.formatted(.dateTime.month(.abbreviated).day())`, or a DateFormatter with `setLocalizedDateFormatFromTemplate("MMMd")` and `.locale = .autoupdatingCurrent`. Consider replacing the Today/Yesterday/MMM-d logic en… |
| AMENAPP/BereanDailyFormation/BereanDailyFormationFeedView.swift:26 | medium | Displayed date label is built with a hardcoded DateFormatter.dateFormat = "EEEE, MMMM d". This bypasses the user's locale: month/weekday names won't localize correctly and the day/month order is wrong for many locales (… | Use a locale-aware style: `Date().formatted(.dateTime.weekday(.wide).month(.wide).day())` (or a DateFormatter with `dateStyle`/`setLocalizedDateFormatFromTemplate` and `.locale = .autoupdatingCurrent`). |
| AMENAPP/BereanMentorship/BereanMenteeGrowthView.swift:357 | medium | Next-session date shown to the user is built with hardcoded DateFormatter.dateFormat = "EEEE, MMM d 'at' h:mm a". Weekday/month names, the literal English word "at", and the 12-hour/AM-PM clock are all locale-specific a… | Use a locale-aware style such as `date.formatted(.dateTime.weekday(.wide).month(.abbreviated).day().hour().minute())`, or DateFormatter with `dateStyle`/`timeStyle` and `.locale = .autoupdatingCurrent`. The literal "at"… |
| AMENAPP/BereanMentorship/BereanMentorPulseView.swift:292 | medium | Upcoming session date displayed to the user is built with hardcoded DateFormatter.dateFormat = "MMM d, h:mm a". Month abbreviation, the comma separator, the 12-hour clock, and the AM/PM marker are all locale-specific (m… | Use a locale-aware format: `date.formatted(.dateTime.month(.abbreviated).day().hour().minute())` (which respects 12/24-hour preference), or a DateFormatter with `dateStyle = .medium`, `timeStyle = .short`, `.locale = .a… |
| AMENAPP/CharityNavigatorService.swift:39 | medium | Program-expense label built with String(format: "%.0f%% to programs", pct) — the percent sign placement and the surrounding English phrase are hardcoded. Percent formatting and word order are not locale-aware. | Format the percentage with (pct/100).formatted(.percent) or NumberFormatter(.percent) using .current locale, and compose the sentence via a localized format string with a positional placeholder, e.g. String(format: NSLo… |
| AMENAPP/ChurchExperienceSystem.swift:1337 | medium | relativeDate builds relative-time strings by hand ('\(diff)d ago', '\(diff/7)w ago', '\(diff/30)mo ago', 'Today', 'Yesterday') with hardcoded English abbreviations and LTR ordering. | Use RelativeDateTimeFormatter (unitsStyle .abbreviated, locale .autoupdatingCurrent) to produce localized 'x days ago' strings, or a String Catalog plural per unit; do not concatenate abbreviations manually. |
| AMENAPP/ChurchFitScoreService.swift:170 | medium | Distance is built with String(format: "%.1f mi", ...) for the user-facing church `topReason`. The literal "mi" hardcodes imperial units (kilometers expected in most locales) and the format's '.' decimal separator is not… | Use MeasurementFormatter / Measurement<UnitLength> with .current locale and .providedUnit (or naturalScale), e.g. Measurement(value: Double(distanceMiles), unit: UnitLength.miles).formatted(.measurement(width: .abbrevia… |
| AMENAPP/ChurchJourneyTimelineView.swift:147 | medium | Displayed milestone date uses DateFormatter with a hardcoded .dateFormat = "MMM d", not locale-aware for visible UI text. | Use date.formatted(.dateTime.month(.abbreviated).day()) or setLocalizedDateFormatFromTemplate("MMMd") so month/day ordering follows the user's locale. |
| AMENAPP/ChurchModels.swift:77 | medium | Displayed distance is built with String(format: "%.1f mi" / "%.0f mi"), hardcoding the imperial "mi" unit and using a non-locale-aware decimal separator. Users in metric/RTL locales see English "mi" and a period decimal… | Build the value with a MeasurementFormatter / Measurement<UnitLength>(value: meters, unit: .meters).formatted(), which applies the user's locale unit system and decimal separator, instead of hardcoding "mi" and %f. |
| AMENAPP/ChurchPillCard.swift:218 | medium | Distance is rendered with String(format: "%.1f mi", distance): the 'mi' unit is hardcoded (wrong for metric-locale users) and the number uses a non-locale decimal separator. | Use Measurement<UnitLength>(value: distance, unit: .miles).formatted(.measurement(width: .abbreviated)) with the current locale, or at minimum distance.formatted(.number) for the value; this localizes both the unit and… |
| AMENAPP/ChurchRadarView.swift:231 | medium | Distance is displayed with String(format: "%.1f mi", ...), hardcoding the 'mi' unit and a non-locale-aware decimal, so metric-locale users see imperial units and a wrong decimal separator. | Use Measurement<UnitLength>(value: distanceMiles, unit: .miles).formatted() (which localizes unit + number) or a NumberFormatter with .locale = .autoupdatingCurrent plus a localized unit string. |
| AMENAPP/ChurchSearchService.swift:67 | medium | Distance string is built with hardcoded 'mi' unit and String(format: "%.1f mi") / "< 1 mi". Users in metric locales see miles and an unlocalized decimal separator; the unit and threshold are English/imperial-only. | Use Measurement<UnitLength>(value: ..., unit: .miles).formatted(.measurement(width: .abbreviated)) with the current locale (which converts to km where appropriate) instead of hardcoding 'mi' and String(format:). |
| AMENAPP/ChurchSmartFeaturesSystem.swift:836 | medium | Distance label hardcodes the ' mi away' unit and uses String(format: "%.1f") with a non-locale decimal separator. Users in metric locales see miles and wrong separators. | Use Measurement(value: dist, unit: UnitLength.miles).formatted(.measurement(width: .abbreviated)) (which honors the user's locale/unit preferences), interpolated into a localized 'X away' format string. |
| AMENAPP/ChurchSmartFeaturesSystem.swift:1059 | medium | Service-countdown text 'Next service in %.0fh' and 'Service in \(mins)m' hardcode English unit abbreviations and ordering; not locale-aware relative/duration formatting. | Use a Measurement<UnitDuration>.formatted or DateComponentsFormatter (unitsStyle .abbreviated) for the duration, embedded in a localized format string, so units and word order localize. |
| AMENAPP/ConnectForYouView.swift:242 | medium | Event dates displayed to the user are built with a hardcoded DateFormatter.dateFormat = "MMM d, h:mm a". This forces US-English month abbreviations and a 12-hour clock regardless of the user's locale (e.g. a French or 2… | Use Date.FormatStyle: return date.formatted(.dateTime.month(.abbreviated).day().hour().minute()) (which respects the locale's 12/24h and month names), or a DateFormatter with .dateStyle/.timeStyle instead of an explicit… |
| AMENAPP/ConnectForumView.swift:463 | medium | Displayed thread/reply timestamps are built with a hardcoded DateFormatter pattern "MMM d, h:mm a". This ignores the user's locale: non-English locales will see English month abbreviations, and 24-hour-clock regions wil… | Use a locale-aware style instead of a literal pattern, e.g. `date.formatted(.dateTime.month(.abbreviated).day().hour().minute())` or a DateFormatter with `dateStyle = .medium; timeStyle = .short` (no .dateFormat). Drop… |
| AMENAPP/ConnectMarketplaceView.swift:216 | medium | Listing price is rendered by manually concatenating a hardcoded "$" with String(format: "%.2f", price). This hardcodes USD, uses a period decimal separator, and puts the symbol on the left — all wrong for many locales/c… | Use a currency style: Text(listing.price, format: .currency(code: "USD")) (or the listing's real currency code). This places the symbol/separators per locale. |
| AMENAPP/ConversationThreadView.swift:1644 | medium | Displayed fallback timestamp uses DateFormatter with a hardcoded .dateFormat = "MMM d". This ignores the user's locale month-name ordering/format conventions for visible UI text. | Use a locale-aware style, e.g. date.formatted(.dateTime.month(.abbreviated).day()) or DateFormatter().setLocalizedDateFormatFromTemplate("MMMd"). |
| AMENAPP/CreatorOS/CreatorMonetizationView.swift:131 | medium | Monthly revenue is shown by concatenating literal "$" with String(format: "%.2f", totalMonthlyRevenue), hardcoding USD symbol position and decimal separator. | Text(totalMonthlyRevenue, format: .currency(code: "USD")) so currency symbol, grouping and decimal separators follow the user's locale. |
| AMENAPP/CreatorSubscriptionGateView.swift:109 | medium | CTA label interpolates the hardcoded-dollar priceFormatted and an English "/mo" suffix: "Subscribe \(priceFormatted)/mo". Wrong currency formatting and untranslatable suffix. | Use locale-aware currency formatting for the price and a localized format string for the whole label (e.g. NSLocalizedString("Subscribe %@/mo", ...)). |
| AMENAPP/CreatorSubscriptionGateView.swift:295 | medium | Subscribe button label hardcodes "$" before a String(format: "%.2f") price plus English "/mo". | Replace with locale-aware currency formatting and a localized format string for the suffix. |
| AMENAPP/CreatorSubscriptionGateView.swift:310 | medium | Confirmation alert message hardcodes "$" and "%.2f" formatting plus English "/month. Cancel anytime." | Format the amount via .formatted(.currency(code:)) / currency NumberFormatter and move the sentence into a localizable format string. |
| AMENAPP/CreatorView.swift:778 | medium | Follower count abbreviation built with String(format: "%.1fk", ...) — the decimal separator and the "k" suffix are hardcoded. Many locales use a comma decimal separator and a different large-number abbreviation; this is… | Use Int(n).formatted(.number.notation(.compact)) (locale-aware compact notation) instead of manual "%.1fk". Same pattern repeats at CreatorView.swift:882 and CreatorView.swift:1245. |
| AMENAPP/DiscoverSearchUpgrade.swift:181 | medium | M/K abbreviations and the decimal separator are hardcoded via String(format:), so non-English/non-US locales see a wrong separator (e.g. should be 1,5M in many locales) and English-only magnitude suffixes. | Use NumberFormatter with numberStyle = .decimal (or iOS 16+ .formatted(.number.notation(.compactName))) and locale = .autoupdatingCurrent to produce locale-correct grouping/abbreviation. |
| AMENAPP/DiscoverySearchResultsView.swift:574 | medium | Church distance is displayed with String(format: "%.1f mi", dist): the decimal separator is hardcoded to '.' (wrong for e.g. fr/de which use ','), the unit 'mi' is a hardcoded English abbreviation, and miles are forced… | Use Measurement with MeasurementFormatter (or .formatted(.measurement(width: .abbreviated))), e.g. Text(Measurement(value: dist, unit: UnitLength.miles).formatted(.measurement(width: .abbreviated, usage: .road))) so the… |
| AMENAPP/DiscussionThreadView.swift:774 | medium | The displayed comment timestamp for older comments uses a DateFormatter with a literal .dateFormat = "MMM d". A hardcoded display format ignores locale month-name ordering and conventions (e.g. day-before-month locales). | Use a locale-aware style instead of a fixed pattern, e.g. date.formatted(.dateTime.month(.abbreviated).day()) or a DateFormatter with setLocalizedDateFormatFromTemplate("MMMd") so the field order follows the current loc… |
| AMENAPP/EnhancedQuietHoursView.swift:188 | medium | Quiet-hours start/end times are stored and displayed as 24-hour "HH:mm" strings (e.g. "22:00") via timeString(from:) and shown directly. Users in 12-hour-clock locales see a 24h time that doesn't match their system cloc… | Store minutes (already done) but format for display with a locale-aware time style, e.g. build a Date from the components and use date.formatted(.dateTime.hour().minute()) or DateFormatter with timeStyle = .short, so 12… |
| AMENAPP/EventsView.swift:941 | medium | shortDate() builds displayed event dates with a hardcoded dateFormat "MMM d, yyyy". This forces US month-day-year ordering for all locales; e.g. German/French users expect day-first, and the month abbreviation is not lo… | Use a localized style instead of a fixed pattern: `date.formatted(.dateTime.month(.abbreviated).day().year())` or `DateFormatter` with `dateStyle = .medium` (which respects locale). |
| AMENAPP/EventsView.swift:501 | medium | monthAbbrev uses a literal dateFormat "MMM" then .uppercased() for the date block shown on each event card. The month abbreviation is not localized and uppercasing is wrong/garbled in many languages. | Use `event.startDate.formatted(.dateTime.month(.abbreviated))` and avoid forced uppercasing, or apply `.uppercased(with: Locale.current)` only if a localized abbreviation is acceptable. |
| AMENAPP/FeaturedTestimoniesManager.swift:102 | medium | formattedNextRotation, displayed in RotationCountdownView, uses a hardcoded dateFormat "MMM d" that is not locale-aware. | Use nextRotationDate.formatted(.dateTime.month(.abbreviated).day()) so the abbreviated month and ordering follow the user's locale. |
| AMENAPP/FileAttachmentHandler.swift:286 | medium | Displayed file size is built with String(format: "%.1f KB"/"%.1f MB", ...) and a raw "\(size) B". This hardcodes the decimal separator ("." vs ","), the unit spacing/labels, and the 1024-base convention instead of using… | Use ByteCountFormatter (or .formatted(.byteCount(style: .file))) which localizes the number, decimal separator, and unit labels: ByteCountFormatter.string(fromByteCount: Int64(size), countStyle: .file). |
| AMENAPP/FindChurch2AdminPortalView.swift:804 | medium | Service-time display string manually builds 12-hour clock with hardcoded 'AM'/'PM' via String(format:"%d:%02d %@"). The displayTime is user-facing ('Sunday at 9:00 AM'). Locales using a 24-hour clock or localized AM/PM… | Build a Date/DateComponents for the hour+minute and format with .formatted(.dateTime.hour().minute()), letting the locale decide 12/24-hour and AM/PM markers. |
| AMENAPP/FindChurchOS/FindChurch2MapListView.swift:293 | medium | List-row distance shown as String(format: "%.1f mi", miles) hardcodes miles and a period decimal separator; metric-locale users get imperial units and wrong separators. | Render with Measurement(value: miles, unit: UnitLength.miles).formatted(.measurement(width: .abbreviated, usage: .road)) so the unit and number localize. |
| AMENAPP/FindChurchOS/FindChurch2SmartChurchCard.swift:401 | medium | Displayed distance uses String(format: "%.1f mi", miles) / "%.0f mi" — hardcodes the 'mi' unit (no metric/km for metric locales) and a period decimal separator. | Use Measurement<UnitLength>(value: miles, unit: .miles).formatted(.measurement(width: .abbreviated, usage: .road)) which converts to km and localizes the number for metric users. |
| AMENAPP/FirstVisitCompanionView.swift:263 | medium | monthLabel builds the calendar header with a hardcoded DateFormatter().dateFormat = "MMMM yyyy". This is displayed text; the literal pattern ignores locale month-name ordering and the user's calendar/region. | Use a locale-aware template: displayMonth.formatted(.dateTime.month(.wide).year()), or set formatter.setLocalizedDateFormatFromTemplate("MMMMyyyy") with formatter.locale = .current. |
| AMENAPP/FirstVisitCompanionView.swift:1139 | medium | formattedSelectedDate (the Date accordion meta shown to the user) uses a hardcoded DateFormatter().dateFormat = "MMM d", which forces English month-before-day ordering regardless of locale. | Use viewModel.selectedDate.formatted(.dateTime.month(.abbreviated).day()) or setLocalizedDateFormatFromTemplate("MMMd") with locale = .current. |
| AMENAPP/FollowerAvatarStack.swift:22 | medium | compactFormatted builds abbreviated counts via String(format: "%.1fM"/"%.1fK"), hardcoding the 'K'/'M' suffixes and using a non-locale-aware decimal separator. The displayed follower count is wrong in locales that use '… | Use NumberFormatter with numberStyle = .decimal and locale = .autoupdatingCurrent, or iOS 'Number.notation(.compactName)' via value.formatted(.number.notation(.compactName)) which localizes both the separator and the K/… |
| AMENAPP/FollowerIntegrationHelper.swift:350 | medium | formatCount builds abbreviated follower counts with String(format: "%.1fM" / "%.1fK"). The decimal separator is forced to "." (wrong in locales that use ",", e.g. de/fr) and the M/K suffixes are hardcoded English abbrev… | Use compact number formatting: count.formatted(.number.notation(.compact)) (or NumberFormatter with .numberStyle = .decimal and .locale = .current), which produces locale-correct separators and abbreviations. |
| AMENAPP/FollowersAnalyticsView.swift:267 | medium | Engagement rate percentage is built with String(format: "%.1f%%"), hardcoding the '%' symbol position and a non-locale-aware decimal separator for displayed text. | Use (viewModel.engagementRate / 100).formatted(.percent.precision(.fractionLength(1))) or NumberFormatter with numberStyle = .percent and locale = .autoupdatingCurrent. |
| AMENAPP/FollowersAnalyticsView.swift:283 | medium | Follower/following ratio is built with String(format: "%.2f"), which uses a non-locale-aware decimal separator (e.g. shows '1.50' where many locales expect '1,50'). | Use viewModel.followerRatio.formatted(.number.precision(.fractionLength(2))) or a NumberFormatter with locale = .autoupdatingCurrent. |
| AMENAPP/FollowersAnalyticsView.swift:570 | medium | Engagement trend string concatenates a '+', a String(format: "%.1f") number, and a literal '% vs last period', hardcoding both the decimal separator and the percent-sign placement for displayed text. | Format the number with .formatted(.number...) or a localized percent style, and move the surrounding text into a localized format string with a positional placeholder so '+' and '%' placement is translatable. |
| AMENAPP/GivingInAppSheet.swift:152 | medium | Preset amount tiles render currency by manual string interpolation: `"$\(Int(amount))"`. The dollar sign is hardcoded and not formatted as currency, so it is wrong for any non-USD locale and ignores locale grouping/symb… | Use a currency style: `amount.formatted(.currency(code: "USD"))` (or the campaign's currency) so symbol placement and grouping follow the user's locale. |
| AMENAPP/GivingInAppSheet.swift:230 | medium | Fee summary amounts are built with String(format: "$%.2f", ...) (gift, platform fee, total on lines 230/243/252). The currency symbol and decimal/grouping separators are hardcoded US-style and ignore the user's locale. | Use `effectiveAmount.formatted(.currency(code: "USD"))` for each line so the symbol and separators are locale-correct. |
| AMENAPP/GreetingSettingsView.swift:255 | medium | Displayed birthday text is built with a hardcoded DateFormatter dateFormat "MMMM d", which is not locale-aware (wrong month-name order/separators for non-English locales). | Use birthday.formatted(.dateTime.month(.wide).day()) (or a Date.FormatStyle), which respects the current locale. Keep the separate "yyyy-MM-dd" parse/store path unchanged. |
| AMENAPP/GrowthArcCardView.swift:88 | medium | dateRangeString, shown on the growth-arc card, builds displayed text with hardcoded dateFormat "MMM yyyy" (not locale-aware). | Use start.formatted(.dateTime.month(.abbreviated).year()) so the month abbreviation and field order follow the locale. |
| AMENAPP/HeyFeedActiveRequestsView.swift:404 | medium | Relative time is built manually with hardcoded English unit suffixes ("s"/"m"/"h"/"d") via string interpolation, which is not localized and ignores RTL/locale relative-date conventions. | Use RelativeDateTimeFormatter (unitsStyle = .abbreviated, locale = .autoupdatingCurrent) or Date.now.formatted(.relative(presentation:)) so units and ordering are localized. |
| AMENAPP/JobModels.swift:81 | medium | Currency fallback uses a hardcoded "$" prefix: "$\(Int(min))". When NumberFormatter returns nil the displayed salary is forced into US dollars regardless of salaryCurrency or locale. | Drop the manual "$" fallback; rely on the NumberFormatter (already configured with .currency + currencyCode) and provide a localized number-only fallback via formatter.string(from:) without a literal symbol, or set form… |
| AMENAPP/JobRecruiterDashboardView.swift:437 | medium | Subscription price built by manually concatenating a hardcoded '$' and '/mo' around String(format: "%.0f"). Wrong currency symbol/placement and decimal separator in non-USD/RTL locales. | Use a currency FormatStyle, e.g. monthlyPrice.formatted(.currency(code: "USD").precision(.fractionLength(0))) and a localized '%@/mo' string, or a localized 'per month' phrasing so symbol placement and separators follow… |
| AMENAPP/MessageGroupingHelper.swift:101 | medium | Displayed message day name uses DateFormatter with hardcoded .dateFormat = "EEEE" without an explicit locale (relies on default but the literal pattern bypasses localized templating); acceptable but the adjacent 'MMM d'… | Replace the literal 'MMM d' and 'MMM d, yyyy' display patterns with self.formatted(.dateTime.month(.abbreviated).day()) and (...).year(), or setLocalizedDateFormatFromTemplate("MMMd") / ("MMMdyyyy"), so date component o… |
| AMENAPP/ModelsUser.swift:80 | medium | formatCount abbreviates counts with String(format: "%.1fK" / "%.1fM"), forcing a '.' decimal separator and English K/M suffixes for displayed follower/post counts; wrong separator in locales using ',' and untranslated m… | Use locale-aware compact formatting: count.formatted(.number.notation(.compactName)) which produces locale-correct separators and magnitude suffixes. |
| AMENAPP/MonetizationToolsView.swift:255 | medium | MRR is displayed via String(format: "$%.0f"), hardcoding the US dollar symbol and US grouping/decimal conventions regardless of locale. | Use a locale-aware currency formatter, e.g. mrr.formatted(.currency(code: "USD")) or NumberFormatter with .numberStyle = .currency and .locale = .current. |
| AMENAPP/MonetizationToolsView.swift:361 | medium | Digital good price is shown with String(format: "$%.2f"), hardcoding the dollar sign and decimal separator and ignoring locale formatting. | Use good.price.formatted(.currency(code: "USD")) (or a NumberFormatter with .currency style and .locale = .current) so the symbol and separators localize. |
| AMENAPP/PeopleDiscoveryView.swift:906 | medium | Post counts are formatted with `String(format: "%.1fM posts", ...)` / `"%.1fK posts"`. The number is not locale-grouped (decimal separator is hardcoded to a period; many locales use a comma) and the "M"/"K" suffix and "… | Use a locale-aware compact formatter: `let f = NumberFormatter(); f.numberStyle = .decimal; f.locale = .current` for the value, or on iOS 16+ `count.formatted(.number.notation(.compactName))` which localizes both the ma… |
| AMENAPP/PostSchedulingSystem.swift:286 | medium | pillLabel() uses DateFormatter literal .dateFormat = "EEE h:mm a" for the 'Scheduled · …' pill — hardcoded English weekday abbreviation and 12-hour clock, not locale-aware. | self.formatted(.dateTime.weekday(.abbreviated).hour().minute()) to localize weekday and clock format. |
| AMENAPP/PostSchedulingSystem.swift:292 | medium | chipLabel() uses DateFormatter literal .dateFormat = "EEE h:mm a" for the ScheduledStatusChip 'Scheduled · …' text — same hardcoded English weekday + 12-hour issue. | self.formatted(.dateTime.weekday(.abbreviated).hour().minute()). |
| AMENAPP/PrayerReminderSettingsView.swift:659 | medium | Custom-reminder time is displayed by manually computing 12-hour value and concatenating AM/PM via String(format: "%d:%02d %@", ...). This hardcodes the 12-hour clock and English 'AM'/'PM' even though many locales use a… | Build a Date from hour/minute and display with a locale-aware time style, e.g. Text(Calendar.current.date(bySettingHour: reminder.hour, minute: reminder.minute, second: 0, of: Date())!, format: .dateTime.hour().minute()… |
| AMENAPP/SafetyCheckOverlayView.swift:204 | medium | The safety-score percentage shown to the user is built with a hardcoded String(format: "%.0f%%", ...). The literal "%" and the '.' decimal grouping are not locale-aware (some locales place the percent sign differently o… | Use a percent NumberFormatter or Foundation's percent format style, e.g. Text((safetyScore).formatted(.percent.precision(.fractionLength(0)))) which renders the locale-correct percent sign and spacing. |
| AMENAPP/SmartNotificationSettingsView.swift:713 | medium | The quiet-hours time picker formats the user-selected time for display with a hardcoded "HH:mm" format, forcing 24-hour display even for users whose locale/region prefers 12-hour (AM/PM). This value is shown back to the… | For the displayed value use a locale-aware time style (e.g. `date.formatted(date: .omitted, time: .shortened)`). If "HH:mm" must stay as the stored/wire format, keep it only for persistence and format separately for dis… |
| AMENAPP/SuggestedAccountCard.swift:341 | medium | Abbreviated follower counts are formatted with String(format: "%.1fM"/"%.1fK") and a bare "\(count)", which use a non-locale decimal separator and hardcoded English K/M suffixes for user-visible numbers. | Use count.formatted(.number.notation(.compact)) (or a NumberFormatter with numberStyle = .decimal and locale = .autoupdatingCurrent) so the decimal separator and compact suffixes are localized. |
| AMENAPP/UnifiedChatView.swift:5244 | medium | Displayed abbreviated count built with String(format: "%.1fK", value): the "." decimal separator is hardcoded (wrong in locales using ",") and the "K"/"M" suffixes are non-localized English magnitude abbreviations. | Use a locale-aware compact number style, e.g. count.formatted(.number.notation(.compactName)) which yields locale-correct decimal separators and localized magnitude suffixes. |
| AMENAPP/VergeCreateRoomSheet.swift:204 | medium | Displayed ticket price is built with String(format: "$%.0f", ticketPrice), hardcoding the USD symbol and US currency placement/grouping for a user-facing amount. | Use ticketPrice.formatted(.currency(code: "USD")) (or the room's actual currency code) so symbol position and grouping follow the locale. |
| AMENAPP/VergeRoomCardView.swift:143 | medium | Ticket price label built with String(format: "$%.0f to join", price): hardcoded leading '$', no locale decimal separator, and fixed English/LTR word order that mis-renders in RTL or non-USD locales. | Format the amount with price.formatted(.currency(code: "USD").precision(.fractionLength(0))) and wrap with a localized '%@ to join' string so currency placement and phrasing localize. |
| AMENAPP/WalkWithChristView.swift:1399 | medium | hourLabel uses a hardcoded DateFormatter.dateFormat = "h a" to render the displayed reminder time (both the picker rows and the 'Daily at <time>' label). Not locale-aware — 24-hour-clock locales will see a wrong AM/PM-f… | Replace the fixed pattern with `fmt.dateStyle = .none; fmt.timeStyle = .short` (respects the device 12/24-hour setting) or `date.formatted(.dateTime.hour())`. |
| AMENAPP/AIInsightsPanelView.swift:75 | low | Displayed percentage built with String(format: "%.0f%%", score * 100), which hardcodes the '%' symbol and uses a non-locale-aware decimal/percent representation. | Use a locale-aware percent format: Text(score.formatted(.percent.precision(.fractionLength(0)))) which places the percent sign correctly per locale. |
| AMENAPP/AIIntelligence/BereanTranslationView.swift:130 | low | The displayed confidence percentage is built with String(format: "%.0f%%", confidence * 100), which uses a hardcoded '%' and no locale-aware percent formatting (some locales space or position the percent sign differentl… | Use (chunk.confidence).formatted(.percent.precision(.fractionLength(0))) so the percent renders per the user's locale. |
| AMENAPP/AMENAPP/AMENAPP/CommunicationOS/.../ChurchProfileView.swift:1218 | low | Within PlanMyVisitView, the service-summary sub-label concatenates a service name and a date with a hardcoded middle-dot: "\(selectedService) · \(selectedDate.formatted(...))". The ordering and separator are fixed LTR;… | Compose via a localized format string with positional placeholders (e.g. "%1$@ · %2$@") so translators control ordering/separator, rather than manual interpolation. |
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCovenantAnalyticsView.swift:252 | low | Percentage built by manual integer math and string concatenation "(\(Int(proportion * 100))%)", appending a literal "%". The percent sign and number formatting are not locale-aware (some locales place the percent sign d… | Use proportion.formatted(.percent.precision(.fractionLength(0))) which renders a locale-correct percent string and digit set. |
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCovenantRoomsView.swift:482 | low | covenantRelativeFormatted() falls back to a hardcoded DateFormatter.dateFormat "MMM d" for displayed older-than-a-week timestamps, hardcoding English month-before-day ordering. The earlier short-form units ("m"/"h"/"d")… | Use `setLocalizedDateFormatFromTemplate("MMMd")` for the fallback, and prefer a RelativeDateTimeFormatter (unitsStyle .abbreviated) for the under-a-week branch so abbreviations localize. |
| AMENAPP/AMENAPP/AMENAPP/ObjectHub/AmenMentorChannelView.swift:1390 | low | EventRailCard day/month blocks use hardcoded DateFormatter.dateFormat "d" and "MMM" for displayed calendar-block text. "MMM" produces an English-only abbreviation and is not localized. | Use locale-aware templates: `setLocalizedDateFormatFromTemplate("d")` and `setLocalizedDateFormatFromTemplate("MMM")`, or `date.formatted(.dateTime.month(.abbreviated))` / `.day()`. |
| AMENAPP/AMENAPP/Catalog/AskCreatorBar.swift:78 | low | Confidence percentage is built with String(format: "%.0f%% confidence", result.confidence * 100). This hardcodes the '%' symbol position and the English word 'confidence', and bypasses locale-aware percent formatting (s… | Format the number with NumberFormatter(numberStyle:.percent, locale:.autoupdatingCurrent) on result.confidence, then place it into a localized format string with a positional placeholder, e.g. String(format: NSLocalized… |
| AMENAPP/AMENAPP/CommunityOS/Church/ChurchObjectHub.swift:536 | low | Event day-number date badge is built with DateFormatter().dateFormat = "d" for displayed text. While day-of-month is fairly locale-stable, using a fixed-format DateFormatter for a displayed value is non-locale-aware and… | Use Date.FormatStyle: event.startDate.formatted(.dateTime.day()) so the displayed value follows the user's locale/calendar. |
| AMENAPP/AMENAPP/CommunityOS/Moderation/AmenCommunityModerationDashboardView.swift:697 | low | The resolution rate is rendered as a percentage with String(format: "%.0f%%", signal.resolutionRate * 100). The percent sign placement and decimal/grouping separators are hardcoded and not locale-aware (some locales pla… | Use a locale-aware percent formatter, e.g. signal.resolutionRate.formatted(.percent.precision(.fractionLength(0))) or NumberFormatter with numberStyle = .percent and locale = .autoupdatingCurrent. |
| AMENAPP/AMENAPP/CommunityOS/Moderation/AmenCommunityModerationDashboardView.swift:705 | low | Average resolution time is displayed via String(format: "%.1f hours", signal.averageResolutionHours). The decimal separator is forced to a period and the "hours" unit word is hardcoded English with no pluralization, so… | Use Measurement<UnitDuration>(value: signal.averageResolutionHours, unit: .hours).formatted() or a localized format string with a number formatted via .number, which yields a locale-correct decimal separator and transla… |
| AMENAPP/AMENAPP/ConnectSpaces/AmenSpaceHeroHeaderView.swift:143 | low | Member count abbreviation built with String(format: "%.1fK") uses a non-locale-aware decimal separator for displayed text. | Use NumberFormatter with .numberStyle = .decimal and .locale = .autoupdatingCurrent (or count.formatted(.number)) instead of String(format:) so the decimal separator and grouping localize. |
| AMENAPP/AMENAPP/ConnectSpaces/AmenSpaceNowLiveMiniPlayer.swift:63 | low | Participant count is abbreviated with String(format: "%.1fK", ...) which both hardcodes the English "K" suffix and uses a period decimal separator that ignores locales using a comma (e.g. "3,2K" expected in many Europea… | Use a locale-aware compact number format: participantCount.formatted(.number.notation(.compactName)) (iOS 16+), which localizes both the separator and the magnitude suffix. |
| AMENAPP/AMENAPP/ConnectSpaces/Connect/AmenConnectPlayerView.swift:292 | low | Confidence percentage displayed in the scripture ref row is assembled with String(format: "%.0f%% confidence", ...). The % symbol is hardcoded and not produced by a locale-aware percent style, so symbol position/spacing… | Format the value with `.formatted(.percent.precision(.fractionLength(0)))` (or NumberFormatter percent style with `.locale = .autoupdatingCurrent`) and use a localized format string with a positional placeholder for the… |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenSpacePaywallView.swift:180 | low | Savings percentage built as "Save \(savingsPct)% annually" with a manually appended "%" sign; percent symbol placement and number formatting are not locale-aware. | Use Double(savingsPct)/100 .formatted(.percent) inside a localized format string so the % placement and digits follow the locale. |
| AMENAPP/AMENAPP/Covenant/AmenCovenantRevenueView.swift:276 | low | Fallback path manually prepends a hardcoded "$" and casts to Int, producing a non-locale-aware, un-grouped currency string when the formatter returns nil. | Use a locale-aware fallback such as value.formatted(.currency(code: "USD")) or value.formatted(.number) rather than concatenating a literal "$". |
| AMENAPP/AMENAPP/DiscoveryOS/DiscoveryCardView.swift:313 | low | Compact member count String(format: "%.1fK", ...) hardcodes "K" and decimal separator; not locale-aware. | Format with NumberFormatter (locale = .autoupdatingCurrent) or compact notation. |
| AMENAPP/AMENAPP/DiscoveryOS/DiscoveryPreviewSheet.swift:305 | low | Compact count formatted with String(format: "%.1fK", ...) — hardcoded "K" abbreviation and decimal separator, not locale-aware. | Use a NumberFormatter with locale = .autoupdatingCurrent (or compact notation) for the abbreviated count. |
| AMENAPP/AMENAPP/Intelligence/ChurchPulseModels.swift:42 | low | formattedNextEventDate creates a DateFormatter without setting .locale. While dateStyle/timeStyle are style-based (good), it omits .locale assignment; relying on the default is usually fine but the surrounding relative-… | Prefer date.formatted(date: .abbreviated, time: .shortened) which is locale-aware by default; no functional change needed beyond the relative-time fix below. |
| AMENAPP/AMENAPP/MediaPostIndexService.swift:288 | low | timeAgoString builds relative time by manual concatenation ("now", "\(n)m", "\(n)h", "\(n)d"). Units are hardcoded English and the output is not localized or RTL-safe; it is surfaced as the post timestamp in MediaDetail… | Replace with RelativeDateTimeFormatter (locale = .autoupdatingCurrent) or date.formatted(.relative(presentation:.numeric)); keep the .medium DateFormatter fallback for older posts (it already respects locale). |
| AMENAPP/AMENAPP/SpiritualOS/Discovery/AmenChurchHeroCard.swift:37 | low | serviceLengthLabel concatenates a raw minute count with literal ' min' rather than locale-aware duration/number formatting. | Use Measurement(value: Double(m), unit: UnitDuration.minutes).formatted() or Duration.seconds(m*60).formatted(.units(allowed: [.minutes])). |
| AMENAPP/AMENAPP/SpiritualOS/Discovery/AmenChurchHeroCard.swift:113 | low | Rating is rendered with String(format: "%.1f", ...), hardcoding the decimal separator; locales that use a comma decimal separator will show the wrong glyph. | Use Text(church.rating, format: .number.precision(.fractionLength(1))). |
| AMENAPP/AMENAPP/SpiritualOS/Discovery/AmenDiscoveryRailsViewModel.swift:233 | low | Upcoming-event badge text is built with hardcoded DateFormatter.dateFormat "MMM d" used as user-visible badge, hardcoding English month-before-day ordering. | Use `dateFormatter.setLocalizedDateFormatFromTemplate("MMMd")` or `startTs.dateValue().formatted(.dateTime.month(.abbreviated).day())`. |
| AMENAPP/AMENAPP/SpiritualOS/LifePlanner/AmenLifePlannerSectionView.swift:25 | low | Displayed short date for Today/Tomorrow rows uses hardcoded DateFormatter.dateFormat = "MMM d" (English month abbreviation, fixed order). | Use date.formatted(.dateTime.month(.abbreviated).day()) or setLocalizedDateFormatFromTemplate("MMMd") so the month name and day order localize. |
| AMENAPP/AMENResourceDetailView.swift:99 | low | Media duration shown to the user is built as "\(m) min" (hardcoded English 'min') or "\(m):\(String(format: "%02d", sec))". The 'min' unit abbreviation is not localized. | For the 'X min' case use Duration/Measurement formatting, e.g. Duration.seconds(s).formatted(.units(allowed: [.minutes], width: .abbreviated)) or Measurement(value: Double(m), unit: UnitDuration.minutes).formatted(); th… |
| AMENAPP/AccountLinkingView.swift:628 | low | Countdown text 'Resend in \(resendTimer)s' concatenates a hardcoded 's' seconds abbreviation and fixed English word order; not localized and not pluralized. | Use a localized format string with a positional %lld placeholder (e.g. NSLocalizedString("Resend in %lld s")) or Duration.seconds(resendTimer).formatted(.units(...)) for locale-aware duration display. |
| AMENAPP/BereanDailyFormation/BereanFormationCardViews.swift:62 | low | Percentage shown as Text("\(pct)%") concatenates a raw integer with a literal '%'. The percent sign placement and the number's digit shaping are not locale-aware (some locales space or position the percent differently a… | Use Text(Double(pct)/100.0, format: .percent) or pct.formatted(.percent.scale(1)) so the percent symbol, spacing and digits localize. |
| AMENAPP/BereanOS/BereanWisdomScoreCard.swift:154 | low | The overall wisdom score shown to the user is formatted via String(format: "%.1f", overallScore), which uses a non-locale decimal separator (shows '8.5' even where the locale expects '8,5'). | Use overallScore.formatted(.number.precision(.fractionLength(1))) so the decimal separator follows Locale.current. |
| AMENAPP/BereanOS/BereanWisdomScoreCard.swift:168 | low | Per-axis scores in the VoiceOver accessibility label are built with String(format: "%.1f", ...), giving a non-localized decimal separator read aloud to the user. | Format each value with .formatted(.number.precision(.fractionLength(1))) so the spoken number matches the user's locale. |
| AMENAPP/BreakTimeNotificationManager.swift:69 | low | The fallback branch of timeString builds displayed time via "\(hour):\(String(format: "%02d", minute))", a hardcoded 24-hour HH:mm layout that ignores the user's locale clock convention (12h vs 24h). The primary path co… | Build the fallback the same locale-aware way (DateComponents -> Date -> DateFormatter with .timeStyle = .short, or Date.FormatStyle), or drop the manual fallback so all displayed times go through the locale formatter. |
| AMENAPP/CalendarIntegrationService.swift:203 | low | DateFormatter with dateFormat = "h:mm a" is used to PARSE the church service time string (e.g. '10:00 AM') without setting a fixed locale. Because the formatter uses the device locale, parsing en-US-formatted 'AM/PM' so… | Pin the parser to a fixed locale since the source string is a stable en-US format: formatter.locale = Locale(identifier: "en_US_POSIX") before setting dateFormat. (If the service time were ever user-entered/displayed, i… |
| AMENAPP/ChurchSmartFeaturesSystem.swift:419 | low | PostVisitReflectionCard.formatDate uses DateFormatter with only dateStyle = .long but no explicit locale; acceptable, however the displayed string relies on default; lower-risk. Flagged for confirmation that .locale sta… | Confirm DateFormatter inherits .current locale (it does by default); optionally migrate to date.formatted(date: .long, time: .omitted) for clarity. Safe as-is. |
| AMENAPP/DiscoverUIEnhancements.swift:505 | low | Follower abbreviation uses String(format: "%.1fM followers"/"%.1fK followers", ...), hardcoding the "." decimal separator and an English unit + suffix in one string. | Format the number with .formatted(.number.notation(.compactName)) (locale-aware) and place it via a localized "%@ followers" format string. (Note: this helper appears unused per the B-018 privacy comment, but if rendere… |
| AMENAPP/DiscussionThreadView.swift:771 | low | Relative comment time is built by manual concatenation: "now", "\(m)m", "\(h)h". This is not localized (units stay English) and not locale/RTL aware. | Use RelativeDateTimeFormatter (with .locale set) or Date.RelativeFormatStyle, e.g. date.formatted(.relative(presentation: .numeric)), which produces correctly localized and RTL-safe short relative strings. |
| AMENAPP/EventsView.swift:494 | low | dayNumber uses literal dateFormat "d" for the displayed day-of-month. While digit shape is mostly fine, this DateFormatter has no locale set, so non-Gregorian calendars (e.g. Buddhist/Persian preferred calendars) will n… | Use `event.startDate.formatted(.dateTime.day())` so the user's calendar/locale is honored. |
| AMENAPP/FindChurch2AdminPortalView.swift:614 | low | Hour picker option labels are built with String(format:"%d %@", h12, ampm) using hardcoded 'AM'/'PM' (line 613). User-facing picker text is English-only and assumes a 12-hour clock. | Format each hour via a Date with .formatted(.dateTime.hour()) so the 12/24-hour presentation and AM/PM markers are localized. |
| AMENAPP/FirebasePostServiceEnhancements.swift:743 | low | formattedEngagementRate displays a percentage via String(format: "%.1f%%", engagementRate * 100), hardcoding the '%' symbol and decimal separator instead of locale-aware percent formatting. | Use engagementRate.formatted(.percent.precision(.fractionLength(1))) or a NumberFormatter with numberStyle = .percent and locale = .current. |
| AMENAPP/HeyFeedActiveRequestsView.swift:279 | low | The displayed score is built with String(format: "%.1f score", ...), which uses a non-locale decimal separator (e.g. should be "1,5" in many locales) and concatenates a hardcoded English word. | Format the number with request.resonanceScore.formatted(.number.precision(.fractionLength(1))) and put the "score" word in a localized format string with a positional placeholder. |
| AMENAPP/JobModels.swift:182 | low | Percentage is built by manual string concatenation: "\(pct)% response rate". The '%' symbol is hardcoded (placement differs by locale) and the value is not formatted via NumberFormatter percent style. | Use NumberFormatter with numberStyle = .percent and locale = .autoupdatingCurrent on responseRate, then place it in a localized format string, e.g. String(localized: "\(percentString) response rate"). |
| AMENAPP/JobRecruiterDashboardView.swift:353 | low | Conversion / response-rate percentages built with String(format: "%.1f%%") (also lines 469 and 668) hardcode '%' and a '.' decimal separator instead of locale-aware percent formatting. | Format with (conversionRate/100).formatted(.percent.precision(.fractionLength(1))) so the percent sign and decimal separator follow the user's locale. |
| AMENAPP/LivestreamOS/PostStreamRecapView.swift:417 | low | formattedDuration renders the recap length as "\(mins) min" by concatenating an integer and a hardcoded English "min" abbreviation, which is not localized or pluralized. | Use a locale-aware duration formatter such as Duration.seconds(recap.durationSeconds).formatted(.units(allowed: [.minutes])) or Measurement<UnitDuration>(value: Double(mins), unit: .minutes).formatted(.measurement(width… |
| AMENAPP/MentorCardView.swift:82 | low | Response time is built by concatenation '~\(responseTimeHours)h reply' with a hardcoded 'h' abbreviation and English word order, not locale-aware and not pluralized. | Use a localized format string (e.g. NSLocalizedString with a %lld placeholder) or Duration/Measurement formatting so the unit and phrasing localize. |
| AMENAPP/Message.swift:232 | low | formattedTimestamp uses a DateFormatter with timeStyle = .short but does not set a locale, relying on default behavior; while .short is locale-style-based, the formatter is recreated each call and locale should be pinne… | Set formatter.locale = .autoupdatingCurrent (style-based timeStyle is otherwise locale-aware), or prefer timestamp.formatted(date: .omitted, time: .shortened). |
| AMENAPP/MessageGroupingHelper.swift:97 | low | 'Yesterday' is a hardcoded English literal used for displayed message timestamps; not localized. | Use NSLocalizedString("Yesterday", comment:) or RelativeDateTimeFormatter for locale-aware relative day naming. |
| AMENAPP/MonetizationToolsView.swift:173 | low | The price input field prepends a literal "$" label before the value, assuming a leading currency symbol; in many locales the symbol is trailing and the separator differs. | Drive the symbol from the locale (or label the field with a localized currency-code string), rather than hardcoding a leading "$". |
| AMENAPP/NewAccountRestrictionService.swift:421 | low | Rate-limit messages embed a reset time formatted with .short style but the resulting string is concatenated into hardcoded English sentences (lines 425-446), and the count limit "(\(limit))" is interpolated raw. The sen… | Move the user-facing messages into localized format strings with positional placeholders (e.g. NSLocalizedString with %1$lld and %2$@ for the localized reset time), keeping `formatter.timeStyle = .short` for the time su… |
| AMENAPP/PeopleDiscoveryView.swift:1438 | low | `formatFollowerCount` uses `String(format: "%.1fM", ...)` / `"%.1fK"`, hardcoding a period decimal separator and an English magnitude suffix. In locales that use a comma decimal separator this renders incorrectly. | Use `Double(count).formatted(.number.notation(.compactName))` (iOS 16+) or a `NumberFormatter` with `.locale = .current` so the separator and the K/M suffix are localized. |
| AMENAPP/PrayerReminderSettingsView.swift:260 | low | Preview reminder times are hardcoded English 12-hour strings ('6:00 AM', '8:00 AM', ...) passed straight into ReminderTimeRow for display. These will show '6:00 AM' even in locales that use a 24-hour clock or localized… | Store the reminder times as hour/minute (or DateComponents) and format each at display time with a localized short time style (Date.FormatStyle .hour().minute() or DateFormatter timeStyle = .short) instead of hardcoded… |
| AMENAPP/ScriptureInsightView.swift:257 | low | Confidence percentage shown to the user is built with String(format: "Confidence: %.0f%%", ...). The percent sign is hardcoded/concatenated rather than formatted via a locale-aware percent style, so the symbol placement… | Use a NumberFormatter with `.numberStyle = .percent` and `.locale = .autoupdatingCurrent` (passing confidence directly, 0.0–1.0), or `connection.confidence.formatted(.percent.precision(.fractionLength(0)))`, and localiz… |
| AMENAPP/SuggestedAccountPeekSheet.swift:351 | low | Count abbreviation uses String(format: "%.1fM"/"%.1fK", ...), hardcoding the "." decimal separator and English compact suffixes regardless of locale. | Use .formatted(.number.notation(.compactName)) or a NumberFormatter with locale = .autoupdatingCurrent so the decimal separator and abbreviation follow the user's locale. |
| AMENAPP/UserProfileView.swift:677 | low | Follower count abbreviation uses String(format: "%.1fK", ...), hardcoding the "." decimal separator and an English "K" suffix; in locales using "," as the decimal separator this renders incorrectly. | Use a NumberFormatter (or .formatted(.number.notation(.compactName))) with locale = .autoupdatingCurrent so the decimal separator and compact suffix follow the locale. |
| AMENAPP/VideoAttachmentHandler.swift:246 | low | Upload progress percent is built manually: "\(Int(progress * 100))%" with a hardcoded '%' symbol and no locale-aware number formatting. | Use progress.formatted(.percent) or NumberFormatter (numberStyle = .percent, locale = .autoupdatingCurrent) so the percent sign placement and digit grouping localize. |
| AMENAPP/WorkflowConfigView.swift:166 | low | Displayed time label is built with String(format: "%02d:00", checkInHour) as a 24h clock. The ':00' and 24h shape are hardcoded; locales using 12h or different time conventions see an unlocalized clock. | Build a Date/DateComponents for the hour and display via Date.FormatStyle (.dateTime.hour().minute()) or DateFormatter with locale = .autoupdatingCurrent so 12h/24h and separators follow the user's locale. |

## 2. RTL (bidirectional / concatenation order)
| File:Line | Severity | Issue | Proposed fix |
|---|---|---|---|
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCovenantDiscoveryView.swift:353 | medium | "Join from \(s)/mo" concatenates a formatted price with a hardcoded "/mo" suffix and English preposition ordering. The price-plus-period phrasing won't reorder or translate correctly in RTL or non-English locales. | Use a localized format string with a positional placeholder, e.g. NSLocalizedString("join_from_price_per_month", ...) formatted with the price, so translators control word order and the "/mo" period abbreviation. |
| AMENAPP/AMENAPP/AMENActivityIntelligenceEngine.swift:326 | medium | Notification titles are assembled by interpolating names and counts directly into English sentence fragments ("\(first) and \(count - 1) others prayed for your request"). The fixed left-to-right ordering of name + conne… | Replace with localized format strings using positional placeholders (e.g. NSLocalizedString with "%1$@ and %2$lld others prayed for your request") and a .stringsdict plural variant for the count, allowing translators to… |
| AMENAPP/AMENAPP/ChurchJourneyPlanner.swift:105 | medium | Displayed sentence is built by manual interpolation "Leave by \(depStr) for \(serviceStr) service". The fixed word order and the trailing literal " service" cannot be reordered for other languages and render incorrectly… | Move to a localized format string with positional placeholders, e.g. String(format: NSLocalizedString("leave_by_for_service", comment: ""), depStr, serviceStr) where the .strings/.xcstrings value is "Leave by %1$@ for %… |
| AMENAPP/AMENAPP/ChurchJourneyPlanner.swift:117 | medium | leaveInSummary builds duration text by manual concatenation with hardcoded unit letters/order: "Leave in \(hours)h \(rem)m" and "Leave in \(minutes) min". Unit ordering and abbreviations are not localized and the h/m su… | Use a locale-aware duration formatter for the magnitude, e.g. DateComponentsFormatter with unitsStyle = .abbreviated (or Duration.formatted on iOS 16+), wrapped in a localized "Leave in %@" format string. |
| AMENAPP/AMENAPP/CommunityOS/Church/ChurchObjectHub.swift:663 | medium | Prayer count built by prepending a literal middle-dot: "· \(item.prayedCount) praying". The hardcoded leading "· " and the manual ordering of count + word render incorrectly under RTL and is not pluralized. | Use a localized format string with a positional placeholder and a .stringsdict plural variant, e.g. NSLocalizedString("prayer.count", ...) with format "%lld praying" and let SwiftUI handle the separator, or build the se… |
| AMENAPP/AMENAPP/CommunityOS/HeroExperience/ContentHeroViewModel.swift:111 | medium | communityCountSummary manually concatenates value+label segments joined by ' · ' in fixed LTR order ('N discussing · N praying · N testimonies'), which renders with wrong ordering/labels under RTL and is not translatabl… | Build each segment from a localized format string with positional placeholders (e.g. "%@ discussing") and join with a localized separator, or expose the three counts to the view for separate localized labels. |
| AMENAPP/AMENAPP/CommunityOS/Modules/PodcastCommunityModule.swift:178 | medium | Host attribution is built by prepending a literal "with " to a comma-joined name list. The leading-word concatenation and ', ' separator are LTR assumptions and untranslatable; in RTL the prefix would sit on the wrong s… | Use a localized format string with a positional placeholder, e.g. Text(String(format: NSLocalizedString("with %@", comment: "podcast hosts"), ListFormatter.localizedString(byJoining: meta.hostNames))). ListFormatter als… |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenSpacePaywallView.swift:172 | medium | Price suffix "/mo" is rendered as a separate Text trailing the price, and the CTA builds "Join — \(price)/mo". Hardcoded "/mo" suffix and manual ordering of price + period render incorrectly under RTL and are untranslat… | Use a single localized format string with a positional placeholder, e.g. NSLocalizedString("price.permonth", value: "%@/mo", ...) so translators control the order and the "per month" phrasing. |
| AMENAPP/AMENAPP/ONE/World/ONEContextGateView.swift:92 | medium | Action button labels append a hardcoded trailing right arrow: "View Source →", "View Label →" (line 117), and "Watch →" (line 196). A literal U+2192 forces left-to-right directionality; in Arabic/Hebrew the call-to-acti… | Drop the literal arrow from the localized string and render direction with an SF Symbol (e.g. Image(systemName: "chevron.forward") / chevron.right) which auto-mirrors under RTL, or use a localized string without the arr… |
| AMENAPP/BereanDailyFormation/BereanFormationCardViews.swift:222 | medium | Prayer subtitle concatenates 'For \(forWhom) · ' with a count-driven 'today' / 'N day(s) ago' phrase. Both the leading 'For', the middot ordering, and the English-only day pluralization (ternary) are LTR/English-bound. | Use a localized format string with positional placeholders for the 'For %@ · %@' pattern and move the 'N days ago' relative phrase to RelativeDateTimeFormatter or a .stringsdict plural. |
| AMENAPP/ConversationThreadView.swift:789 | medium | Share excerpt is built by manual LTR concatenation of quote + em-dash + author name. In RTL locales (Arabic/Hebrew) the dash/attribution order renders incorrectly. | Use a localized format string with positional placeholders, e.g. String(format: NSLocalizedString("quote_attribution_format", comment: ""), selection.text, comment.authorName), letting translators control ordering/punct… |
| AMENAPP/ConversationThreadView.swift:1152 | medium | Nested-reply share excerpt also concatenates quote + em-dash + author name in fixed LTR order, mirroring the issue at line 789. | Use the same localized positional-placeholder format string as the root-comment case so translators control attribution ordering and punctuation. |
| AMENAPP/CreatorOS/CreatorMonetizationView.swift:170 | medium | Tier subtitle manually concatenates currency, a hardcoded "/mo" suffix, a middot, and a pluralized subscriber count: "$X.XX/mo · N subscribers". The fixed left-to-right ordering and hardcoded "$"/"/mo" will render incor… | Build from a localized format string with positional placeholders, e.g. String(format: NSLocalizedString("%@ per month · %@", comment: ""), price.formatted(.currency(code: "USD")), subscriberCountString) and use a .stri… |
| AMENAPP/CreatorSubscriptionGateView.swift:83 | medium | Price and the suffix " /month" are concatenated as two Text views in fixed LTR order with a hardcoded leading space + slash. Under RTL (Arabic/Hebrew) the price/period ordering and the slash position render incorrectly,… | Use a single localized format string with a positional placeholder, e.g. Text(String(format: NSLocalizedString("%@ /month", comment: "price per month"), priceFormatted)) so translators control word order and direction. |
| AMENAPP/EventsView.swift:954 | medium | timeRange() concatenates start and end times with a hardcoded LTR ordering and an en-dash separator: "\(start) – \(end)". In RTL locales (Arabic/Hebrew) the range will render in the wrong visual order. | Use a locale-aware range formatter: `(start..<end).formatted(.interval.hour().minute())` or build the string from a localized format string with positional placeholders. |
| AMENAPP/GivingInAppSheet.swift:317 | medium | The success confirmation builds a sentence with positional substitution but a hardcoded leading '$' and fixed word order: `String(format: "Your $%.0f %@ gift to %@ is confirmed.", ...)`. The currency symbol is hardcoded… | Use a fully localized format string (NSLocalizedString) with the amount pre-formatted via `.currency` and positional placeholders (%1$@, %2$@) so translators control order and the symbol is locale-correct. |
| AMENAPP/JobModels.swift:84 | medium | Salary range and period are assembled by manual interpolation: "\(minStr) – \(maxStr)/\(shortLabel)". The fixed order (min, dash, max, slash, period) and leading/trailing punctuation render incorrectly under RTL (Arabic… | Use a localized format string with positional placeholders, e.g. String(localized: "\(minStr) – \(maxStr)/\(periodLabel)") defined in a String Catalog so translators control ordering and the period suffix. |
| AMENAPP/MentorCardView.swift:113 | medium | Button text appends a literal trailing arrow ('Request →') and a trailing check ('Active ✓'); the → assumes LTR reading order and will point the wrong way / sit on the wrong side in Arabic/Hebrew. | Move the directional glyph into an SF Symbol Image (e.g. 'arrow.forward', which auto-mirrors under RTL) alongside a localized label, or use a localized format string rather than a hardcoded trailing arrow. |
| AMENAPP/NotificationDigestView.swift:261 | medium | A date range for the digest period is built by manual concatenation "\(start) - \(end)" with a hardcoded hyphen separator and fixed start-then-end order. Under RTL this renders the dates and dash in the wrong visual ord… | Use a locale-aware range formatter: Date.IntervalFormatStyle, e.g. (digest.period.start..<digest.period.end).formatted(.interval.day().month().year()) which handles ordering, separator and RTL correctly. If staying with… |
| AMENAPP/SafetyUIComponents.swift:577 | medium | timeUntil builds a relative duration by concatenating "in \(hours)h \(minutes)m" and "in \(minutes)m". The leading "in" word, the h/m unit abbreviations, and the number-before-unit ordering are hardcoded English/LTR and… | Use a locale-aware relative formatter, e.g. RelativeDateTimeFormatter (formatter.localizedString(for: date, relativeTo: Date())) or Duration.formatted / DateComponentsFormatter with unitsStyle = .abbreviated and a local… |
| AMENAPP/AMENAPP/AIIntelligence/BereanPipelineClient.swift:67 | low | Not a defect — conversationHistory dictionary keys are wire payload; flagged only to confirm no user-facing string issue here. (No action.) | No change; this is backend wire data, intentionally locale-independent. |
| AMENAPP/AMENAPP/AMENAPP/BereanPulse/BereanPulseView.swift:410 | low | permissionSummary builds user-facing text by interpolating a count then an English word ("\(blocked) blocked"), assuming LTR number-then-label order and a non-localized, non-pluralized word. | Use a localized format string / stringsdict plural, e.g. String(localized: "\(blocked) blocked") backed by a plural rule so number placement and the word adapt per locale. |
| AMENAPP/AMENAPP/AMENAPP/BereanPulse/BereanPulseView.swift:215 | low | Trust pill value "\(visibleSignalCount) visible" concatenates a count and an English word with assumed LTR ordering and no localization. | Replace with a localized format string with a positional placeholder, e.g. String(localized: "\(visibleSignalCount) visible"), translatable per locale. |
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCreatorHubView.swift:541 | low | HubTierPill price is built by concatenating the formatted currency string with billingPeriod.displayLabel: "\(s)\(tier.billingPeriod.displayLabel)". The currency value and the period label are glued in a fixed LTR order… | Compose with a localized format string with positional placeholders (price, period) rather than raw interpolation, so word/segment order localizes. |
| AMENAPP/AMENAPP/CameraOS/Safety/LocationDelayService.swift:95 | low | User-facing delay descriptions interpolate a formatted time/date inside hardcoded English phrasing with manual parentheses, e.g. "Publishing after the event (\(formatted))". The fixed wrapper text and parenthesis placem… | Replace each branch with a String(localized:) format string containing a positional placeholder, e.g. String(localized: "Publishing after the event (%@)") so translators control wording and RTL bidi handles the parenthe… |
| AMENAPP/AMENAPP/ChurchJourneyPlanViewModel.swift:139 | low | The displayed service label is assembled by manually joining [label, startTime] with " • ", assuming left-to-right order. Under Arabic/Hebrew the label and time render in the wrong visual order. | Use a localized format string with positional placeholders (e.g. NSLocalizedString("%@ • %@")) so the bidi engine can order label and time correctly, rather than hardcoded LTR concatenation. |
| AMENAPP/AMENAPP/CommunityOS/Discussion/AmenDiscussionRoomListView.swift:308 | low | Provenance source hint interpolates a leading literal 'From ' before the value, assuming LTR ordering and not localizable. | Back this with a localized format string "From %@" so translators control ordering and bidirectional rendering is correct in RTL locales. |
| AMENAPP/AMENAPP/CommunityOS/Discussion/AmenDiscussionThreadView.swift:386 | low | Provenance label is built by interpolating a leading literal 'From ' before a value, which assumes left-to-right ordering and is not a localizable format string. | Use a localized positional format, e.g. Text("From \(prov.sourceType.capitalized)") backed by a catalog key "From %@" so translators control word order and bidi runs are handled. |
| AMENAPP/AMENAPP/CommunityOS/Modules/PodcastCommunityModule.swift:166 | low | Episode/season label is built by string concatenation: "Episode \(ep)" + " · Season \(season)". Both the leading number placement and the appended ' · Season N' fragment assume LTR ordering and are not translatable as a… | Use localized format strings with positional placeholders for each variant, e.g. NSLocalizedString("Episode %d", ...) and NSLocalizedString("Episode %1$d · Season %2$d", ...), selected by whether season is present. |
| AMENAPP/AMENAPP/CommunityOS/Monetization/AmenPlanComparisonView.swift:239 | low | The '/mo' suffix is concatenated after the price as a separate appended Text, which assumes left-to-right reading order and is an untranslatable English abbreviation. | Use a single localized format string with a positional placeholder (e.g. "%@ / month" via a String Catalog) so word order mirrors correctly under RTL and the unit is translatable. |
| AMENAPP/AMENAPP/CommunityOS/Org/OrgMemberCountBadge.swift:51 | low | "members" label is rendered as a separate Text immediately trailing the count number, manually ordering number-then-noun. Under RTL this fixed visual order breaks, and it is not pluralized. | Combine into a single localized, pluralized string with a positional placeholder (e.g. "%lld members" via .stringsdict) so order and plural follow the locale. |
| AMENAPP/AMENAPP/ConnectSpaces/Connect/AmenConnectPlayerView.swift:605 | low | Time range strings like "00:02:00–00:03:15" and reference+translation pairs are concatenated/ordered in code. While timestamps are largely neutral, the label+value pairs (reference then translation badge) are positional… | Where reference and translation are shown together as user-facing text, prefer a single localized format string with positional placeholders rather than relying on view/order placement so RTL phrasing stays correct. |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenCreatorEarningsDashboard.swift:740 | low | Accessibility/percentage value "\(Int(split.percentage)) percent" and the visible "%" rendered as a trailing standalone Text manually order number-then-symbol; the displayed integer percentage is not locale-formatted. | Format with (split.percentage/100).formatted(.percent) and use a localized format string for the spoken label so order follows the locale. |
| AMENAPP/AMENAPP/Covenant/AmenCovenantPaywallView.swift:311 | low | formattedPrice concatenates the formatted price directly with billingPeriod.displayLabel ("\(priceStr)\(...displayLabel)"), assuming LTR adjacency; this can render incorrectly under RTL and offers no localizable joining… | Compose price and period with a localized format string with positional placeholders, e.g. String(localized: "\(priceStr)\(label)") defined per locale, rather than raw interpolation. |
| AMENAPP/AMENAPP/Covenant/AmenCovenantRevenueView.swift:223 | low | Count-driven member text built by concatenation "\(count) members" with manual plural noun; no singular handling and no positional localization. Renders incorrectly under RTL and for count == 1. | Use a String Catalog / .stringsdict plural entry keyed on count (e.g. "%lld members" with one/other variants) so plural rule and ordering are locale-driven. |
| AMENAPP/AMENAPP/DiscoveryOS/DiscoveryCardView.swift:300 | low | Growth value "+\(data.growth7d)" manually prepends a "+" sign; sign placement is wrong under RTL and not locale-aware. | Produce the signed number via a locale-aware NumberFormatter rather than string-prepending "+". |
| AMENAPP/AMENAPP/DiscoveryOS/DiscoveryPreviewSheet.swift:245 | low | Label text built by string concatenation "Latest: " + topic, which prepends an English prefix and fixed LTR punctuation that renders incorrectly in RTL. | Use a localized format string with a placeholder, e.g. String(localized: "Latest: %@", topic), so translators control prefix and ordering. |
| AMENAPP/AMENAPP/DiscoveryOS/DiscoveryPreviewSheet.swift:241 | low | Growth stat built as "+\(data.growth7d)" — the leading "+" sign is manually prepended and will sit on the wrong side under RTL; the sign should be produced by a locale-aware number formatter. | Use NumberFormatter with positivePrefix / a signed style (e.g. numberStyle .decimal with plusSign) and locale = .autoupdatingCurrent so the sign placement follows the locale. |
| AMENAPP/AMENAPP/SocialGraph/Services/SmartActivityCopyGenerator.swift:52 | low | Headlines built by prepending an English verb to a relative-time fragment ("Posted \(relativeTime)", "Prayed \(relativeTime)", "Took notes \(relativeTime)", "Active \(relativeTime)") assume LTR word order and a fixed En… | Use a localized format string with a positional placeholder per case (e.g. String(localized: "Posted %@") ) so translators control word order; combine with a localized relative-time value. |
| AMENAPP/AMENAPP/SpiritualOS/Churches/AmenChurchHubView.swift:594 | low | "View All Prayers →" hardcodes a trailing right-arrow which points the wrong direction under RTL locales (Arabic/Hebrew), where the affordance should point left. | Use an SF Symbol chevron (Image(systemName: "arrow.right")) in a Label/HStack so it auto-mirrors under RTL, or a localized string, instead of a literal U+2192 in the text. |
| AMENAPP/AMENAPP/SpiritualOS/Churches/AmenChurchHubView.swift:718 | low | "Start here →" hardcodes a trailing right-arrow inside the displayed text, which will not mirror for RTL layouts and points the wrong way. | Render the arrow as a separate auto-mirroring SF Symbol (chevron.right) rather than embedding U+2192 in the localized string. |
| AMENAPP/AMENAPP/SundayHomeView.swift:274 | low | The verse reference is rendered by prepending a literal em-dash ('— \(ref)'), which assumes LTR placement of the leading punctuation and renders incorrectly in RTL scripts. | Use a localized format string with a positional placeholder (e.g. "— %@" in a String Catalog) so the attribution punctuation can be positioned per locale. |
| AMENAPP/AdaptiveGlassInboxView.swift:472 | low | Greeting strings concatenate an English greeting with a middot and the date ("Good morning · \(date)"), assuming LTR ordering and hardcoding the separator; renders incorrectly in RTL. | Use a localized format string per time band, e.g. String(localized: "Good morning · %@", date), letting translators control ordering and the separator. |
| AMENAPP/AmenHealthyImmersiveMediaSystem.swift:471 | low | Key-moment pill title is concatenated manually as "\(moment.timestampLabel) \(moment.label)", fixing timestamp-before-label order; under RTL the leading timestamp will not flow naturally and the spacing/order is not tra… | Compose via a localized format string, e.g. String(localized: "\(moment.timestampLabel) \(moment.label)"), letting translators control order; or render timestamp and label as separate views in an HStack that mirrors aut… |
| AMENAPP/BereanDailyFormation/BereanFormationCardViews.swift:148 | low | Reading-plan progress 'Day \(currentDay) of \(totalDays) — today's passage' hardcodes English word order and the 'Day ... of ...' structure, which won't translate or mirror correctly. | Use a localized format string: String(format: NSLocalizedString("Day %lld of %lld — today's passage", comment: ""), plan.currentDay, plan.totalDays). |
| AMENAPP/ChurchCapsuleView.swift:34 | low | The date is rendered with a manually prepended leading middle-dot separator ("· \(dateStr)"), which assumes LTR ordering and hardcodes the separator before the value; under RTL the leading separator can render on the wr… | Move the separator into the layout (e.g. a Text("·") sibling in the HStack) or use a localized format string with a positional placeholder so the separator placement follows the layout direction. |
| AMENAPP/ChurchChemistryService.swift:171 | low | Label/value pair is built as two adjacent Text views in an HStack with a trailing-space label ("Chemistry estimate: " followed by the value). Manual left-to-right ordering with hardcoded trailing punctuation does not mi… | Use a single localized format string with a positional placeholder for the value (e.g. NSLocalizedString("Chemistry estimate: %@", ...)) so the layout direction and punctuation follow the locale. |
| AMENAPP/ChurchNotesShareService.swift:228 | low | Share attribution is built by manual concatenation that assumes LTR word order ('— from my notes', ' at \(church)', ' (\(date))', then scripture on a new line). The leading em-dash, appended ' at ', and parenthesized da… | Compose the attribution from a single localized format string with positional placeholders (e.g. NSLocalizedString("shared_from_notes_at_%@_on_%@")) so translators control ordering and bidi rendering; let the date come… |
| AMENAPP/ChurchPillCard.swift:47 | low | Pill subtitle is concatenated as "• \(distance) • \(city)" with hardcoded leading bullet and middle separators. The fixed leading '• ' and LTR ordering render incorrectly in RTL languages (Arabic/Hebrew), and the struct… | Use a localized format string with positional placeholders, e.g. String(localized: "%@ \u{2022} %@") (or a natural localized phrasing), so the separator placement and order follow the locale's direction. |
| AMENAPP/ChurchPillCard.swift:136 | low | City/state are concatenated as "\(city), \(state)" with a hardcoded comma-space. When state is nil/empty it yields a trailing ', ', and the literal comma ordering is not localizable for RTL or for locales that order reg… | Build the address line with a localized format and drop empty components, e.g. [city, state].filter{ !$0.isEmpty }.joined(separator: localizedListSeparator), or a String(localized:) format with positional args. |
| AMENAPP/ChurchSearchService.swift:71 | low | Service time label is composed by interpolation '\(serviceType) \(time)' with fixed LTR ordering, which would render incorrectly for RTL languages. | Use a localized format string with positional placeholders, e.g. String(localized: "%1$@ %2$@") so the type/time order can adapt per locale. |
| AMENAPP/ChurchSearchService.swift:75 | low | Address is concatenated with a hardcoded ', ' separator ("\(address), \(city)"), assuming Western address ordering and LTR comma placement. | Use a localized format string with positional placeholders for address/city so the separator and order can be localized. |
| AMENAPP/DiscoverSearchComponents.swift:517 | low | Event subtitle joins location and a date string with a hardcoded " · " middot separator: `[location, dateStr].joined(separator: " · ")`. The fixed LTR ordering of the two parts will render in the wrong visual order in R… | Compose the two parts with a localized format string (positional placeholders) or wrap each part with bidi isolation so order adapts to the layout direction. |
| AMENAPP/DiscoverSearchUpgrade.swift:116 | low | Display name and follower count are concatenated with a hardcoded ' · ' middle dot separator (person.displayName + " · " + formatFollowers(...)). Manual ordering renders in fixed LTR order and is not localizable. | Use a localized format string with positional placeholders, e.g. String(localized: "%1$@ · %2$@") so translators can reorder; or compose with a localized list/separator. |
| AMENAPP/DiscoverySearchResultsView.swift:290 | low | Empty-state message is built by interpolation: "No \(tab.rawValue.lowercased()) found for \"\(query)\"". The fixed leading 'No', trailing word order, and literal quote marks assume LTR phrasing and won't reorder for Ara… | Move to a localized format string with positional placeholders, e.g. String(format: NSLocalizedString("search.empty.%@.%@", comment: ""), tab.localizedName, query) and let the translation place the tab name and query na… |
| AMENAPP/DiscoveryService.swift:547 | low | Follow-suggestion reason is built as "Posts about \(topicDisplay)" with hardcoded English leading text and LTR ordering; this user-visible reason string is not localized and won't adapt order for RTL/other languages. | Use a localized format string with a positional placeholder, e.g. NSLocalizedString("Posts about %@", ...) with the topic substituted. |
| AMENAPP/EventsView.swift:684 | low | "· \(spotsLeft) spots left" prepends a middot separator and uses English-only plural noun, assuming LTR layout next to the preceding count. | Use a localized format string (with the separator handled by the layout/HStack, not embedded) and a plural variant for "spots left". |
| AMENAPP/FeaturedTestimoniesManager.swift:151 | low | The label manually concatenates count text + " • " + date in fixed left-to-right order, which mirrors incorrectly under RTL (Arabic/Hebrew). | Use a localized format string with positional placeholders (e.g. NSLocalizedString with %@ %@) so translators control ordering and the bullet separator placement. |
| AMENAPP/FindChurch2AdminPortalView.swift:819 | low | Suggestion row composes 'day at time' with a hardcoded English connector and fixed LTR order via interpolation: "\(displayDay) at \(displayTime)". The literal ' at ' and the day-before-time ordering are not localizable… | Use a localized format string with positional placeholders, e.g. String(format: NSLocalizedString("servicetime.day_at_time", comment: ""), displayDay, displayTime), letting translators place the connector and order. |
| AMENAPP/FindChurchOS/FindChurch2SmartChurchCard.swift:320 | low | Distance accessibility/footer label built by concatenating a localized distance string with the hardcoded English suffix ' away' (distanceStr + " away"). The fixed trailing word and LTR order break in RTL and other lang… | Use a localized format string with a placeholder: String(format: NSLocalizedString("%@ away", comment: "distance to church"), distanceStr). |
| AMENAPP/FirstVisitCompanionView.swift:730 | low | Service language and stream tags are rendered by prepending a literal middle-dot separator string "· \(lang)" / "· STREAM", which places the separator on the left and will render incorrectly under RTL. | Use a localized format string with a positional placeholder (e.g. NSLocalizedString("%@ ·", ...)) or let HStack spacing/Text concatenation handle the separator so the layout direction mirrors correctly. |
| AMENAPP/GrowthArcCardView.swift:91 | low | Date range is assembled by manual concatenation with a hardcoded en-dash and a hardcoded "Present" word in fixed LTR order; mirrors wrong under RTL and the dash/word are not localizable. | Use Date.IntervalFormatStyle / start..<end formatted as a range, and a localized "…to present" format string with a positional placeholder instead of manual string building. |
| AMENAPP/MusicContentLayer/ListeningDiscussionRoomView.swift:116 | low | 'Hosted by \(room.hostName)' hardcodes the English leading phrase concatenated with the host name; not translatable and LTR-fixed. | Use a localized format string with a placeholder: String(format: NSLocalizedString("Hosted by %@", comment: "room host"), room.hostName). |
| AMENAPP/PeopleDiscoveryView.swift:1484 | low | Post author label is built as "by \(post.authorName)" — a hardcoded English word prepended to a value with assumed LTR ordering. In RTL locales (Arabic/Hebrew) the leading "by" and the name will not arrange correctly, a… | Use a localized format string with a positional placeholder, e.g. `Text("by_author \(post.authorName)")` backed by a catalog entry "by %@" so translators can reorder, or use markdown/`AttributedString` that respects the… |
| AMENAPP/PhoneVerificationService.swift:176 | low | Masked phone display is built as "••• ••• \(storedPhoneLast4)" with the masking dots hardcoded to the left of the last-4 digits. In RTL locales this fixed left-to-right ordering of the mask + digits can render in an une… | Wrap the masked string with directional control (e.g. format via a localized template) or apply `String.localizedStringWithFormat` so bidi rendering is correct; at minimum mark the run as LTR for the numeric portion. |
| AMENAPP/SearchService.swift:272 | low | Search result subtitle hardcodes a leading "@" before the username ("@\(username)") and "by \(authorName)" (line 369). The prepended symbol/word assumes LTR; in RTL these affixes render on the wrong side and the "by" wo… | For "by \(authorName)" use a localized format string with a positional placeholder. For the @handle, render the handle in a dedicated label or wrap with bidi isolation so the @ stays adjacent in RTL. |
| AMENAPP/SuggestedAccountPeekSheet.swift:234 | low | Mutual-followers label hardcodes the "+" and "others" ordering in LTR: "Followed by \(first.displayName) + \(totalCount - 1) others". The literal " + " and trailing "others" assume left-to-right reading and are not a lo… | Move the whole sentence into a localized format string with positional placeholders, e.g. NSLocalizedString("Followed by %@ and %lld others", ...), letting translators handle word order and direction (ideally combined w… |
| AMENAPP/TrueSourceService.swift:169 | low | The AI-assisted suffix is rendered as a separate Text hardcoding a leading "· " middle-dot prefix appended after the "Verified Original" label. Under RTL (Arabic/Hebrew) the leading separator dot is hardcoded LTR and th… | Express the combined badge as one localized format string with positional placeholders (e.g. "%1$@ · %2$@") so the bidi algorithm and translator control separator placement and ordering, rather than hardcoding the "· "… |
| AMENAPP/VergeCreatorStudioView.swift:222 | low | The subscription-price input manually prepends a literal "$" Text view to the left of the amount field. Under RTL (Arabic/Hebrew) the currency symbol should sit on the trailing side; a hardcoded leading "$" plus hardcod… | Drop the manual "$" Text and format the value with a locale/currency-aware style (e.g. Decimal(...).formatted(.currency(code: ...))), or use a localized format string with a positional placeholder so symbol placement mi… |

## 3. Plurals (count-driven text)
| File:Line | Severity | Issue | Proposed fix |
|---|---|---|---|
| AMENAPP/ChurchToolsView.swift:691 | high | Meeting day is pluralized by appending a literal "s" to the day name: "\(group.meetingDay)s at ...". This only works in English ("Thursdays"); for any other localized weekday it produces a garbled word, and once the day… | Do not build the recurring-day plural by string concatenation. Derive the weekday and format the recurrence with a locale-aware approach (e.g. Calendar weekday symbols plus a localized "every {weekday}" format string),… |
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCovenantAnalyticsView.swift:249 | medium | Manual English pluralization via ternary "member\(count == 1 ? \"\" : \"s\")". Only handles English singular/plural; breaks for languages with other plural rules. | Move to a String Catalog / .stringsdict plural variant keyed on count (one/other/...) so the correct plural form is chosen per locale. |
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCovenantAnalyticsView.swift:305 | medium | Manual English pluralization "post\(count == 1 ? \"\" : \"s\") trending" built by concatenation. Does not localize and only handles English plural rule. | Use a .stringsdict plural entry like "%lld posts trending" with one/other variants instead of code-side ternary concatenation. |
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCovenantRoomDetailView.swift:517 | medium | Reply count pluralized with a code-only ternary: replyCount == 1 ? "reply" : "replies". This only handles English plural rules (no zero/few/many forms) and concatenates count + word, which also mis-orders under RTL. | Move to a String Catalog / .stringsdict plural entry keyed on the count (e.g. "%lld replies" with one/other variants) so each locale supplies its own plural rule and word order. Requires adding the plural rule, not just… |
| AMENAPP/AMENAPP/AMENAPP/ObjectHub/AmenObjectDiscussionRoomView.swift:311 | medium | Presence pill pluralizes via ternary "\(count) \(count == 1 ? \"person\" : \"people\")". This is English-only one/other handling; other locales require distinct plural forms not expressible by a binary ternary. | Use a stringsdict / String Catalog plural variant keyed on participantCount (e.g. "%d people present" with one/other/etc.) via String.localizedStringWithFormat; keep the "Empty"/zero case as its own localized variant. |
| AMENAPP/AMENAPP/AMENAPP/Pulse/AmenPulseSurfaceView.swift:341 | medium | Plural built by concatenation with a code-level singular/plural ternary: count + (count == 1 ? "prompt" : "prompts"). English-only pluralization rule; breaks for languages with different plural categories (e.g. Russian,… | Move to a .stringsdict / String Catalog plural entry, e.g. String(localized: "\(prayerCount) prompts") backed by a plural variant keyed on the count, instead of the inline ternary. |
| AMENAPP/AMENAPP/AMENAPP/Pulse/AmenPulseSurfaceView.swift:346 | medium | Plural built by concatenation with a singular/plural ternary: count + (count == 1 ? "update" : "updates"). English-only pluralization. | Replace with a String Catalog plural variant keyed on communityCount (e.g. "%lld updates" with one/other forms) rather than the inline ternary. |
| AMENAPP/AMENAPP/AMENAPP/Pulse/AmenPulseSurfaceView.swift:355 | medium | User-facing community line builds a plural by concatenation: count + (prayers == 1 ? "prayer request from your groups" : "prayer requests from your groups"). English-only one/other rule. | Use a String Catalog plural entry for the whole phrase keyed on the prayer count, with proper plural categories per locale. |
| AMENAPP/AMENAPP/CommunityOS/Emergence/CommunityEmergenceBannerView.swift:14 | medium | formattedMemberCount hardcodes English singular/plural ('1 member' vs 'N members') with manual concatenation. This produces grammatically wrong output in every language with non-English plural rules (e.g. Russian, Arabi… | Move the member-count string into a String Catalog / .stringsdict entry with a plural variable, e.g. Text("members_count", comment: ...) backed by NSLocalizedString with a %lld plural rule, and let the catalog pick zero… |
| AMENAPP/AMENAPP/CommunityOS/Org/OrgMemberCountBadge.swift:72 | medium | Compact count formatting uses String(format: "%.1fM"/"%.1fK", ...) with hardcoded M/K suffixes and a US-style decimal point; not locale-aware (decimal separator, grouping, abbreviation style differ by locale). | Use a NumberFormatter with numberStyle = .decimal and locale = .autoupdatingCurrent, or value.formatted(.number.notation(.compactName)) which produces locale-correct compact forms. |
| AMENAPP/AMENAPP/ConnectSpaces/AmenCreatorSpaceHeroView.swift:207 | medium | Member count text concatenates an abbreviated number with a hardcoded English ' members' suffix and uses String(format:) for the abbreviated value (non-locale decimal separator). No plural rule and English-only word. | Move 'members' into a .stringsdict / String Catalog plural entry keyed on the count, and format the number via a NumberFormatter (numberStyle .decimal, .locale = .autoupdatingCurrent) or .formatted() so the decimal sepa… |
| AMENAPP/AMENAPP/ConnectSpaces/AmenSpaceDiscoveryView.swift:264 | medium | Member count label "\(space.memberCount) members" uses a hardcoded English plural noun with no plural rule. | Use a .stringsdict / String Catalog plural entry keyed on memberCount (one/other) for the "members" label. |
| AMENAPP/AMENAPP/ConnectSpaces/Events/AmenUpcomingLiveChipView.swift:17 | medium | Countdown text concatenates units: "Starting in \(mins)m" and "Starting in \(hours)h". Hardcoded English m/h abbreviations and manual ordering of word + number + unit; not pluralized or locale-aware. | Use a localized format string with a positional placeholder and locale-aware duration formatting (e.g. DateComponentsFormatter with unitsStyle, or Measurement<UnitDuration>.formatted) so phrasing and order follow the lo… |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenCreatorEarningsDashboard.swift:363 | medium | Member count detail uses manual pluralization: "\(count) member\(count == 1 ? "" : "s")". This only works for English and breaks for languages with different plural rules. | Use a .stringsdict / String Catalog plural variant keyed on the count (e.g. "%lld members") instead of ternary "s" suffixing. |
| AMENAPP/AMENAPP/ConnectSpaces/Monetization/AmenGiftMembershipView.swift:32 | medium | Duration labels ("1 Month", "3 Months", "6 Months", "1 Year") are hardcoded English strings with manual singular/plural forms; not localizable and not plural-rule-correct for other languages. | Derive labels from the numeric months via a localized, plural-aware unit string (.stringsdict for "%lld months"/"%lld years") rather than fixed English literals. |
| AMENAPP/AMENAPP/CreatorProfiles/FeaturedSmartModuleCard.swift:269 | medium | `courseSubtitle` builds "\(moduleCount) modules · \(lessonCount) lessons" with hardcoded English plural nouns and a manually ordered, non-localizable label string. Singular counts ("1 modules") are wrong, and the fixed… | Build each segment from a String Catalog plural entry (modules_count / lessons_count keyed on count) and compose them via a localized format string with positional placeholders rather than manual interpolation. |
| AMENAPP/AMENAPP/DiscoveryOS/DiscoveryCardView.swift:277 | medium | "\(data.replyCount) replies" uses a hardcoded English plural noun with no plural rule. | Use a .stringsdict plural variant keyed on replyCount for the "replies" label. |
| AMENAPP/AMENAPP/DiscoveryOS/DiscoveryPreviewSheet.swift:157 | medium | Speaker count pluralized by appending "s": "\(count) speaker\(count == 1 ? \"\" : \"s\") live". English-only plural handling. | Use a .stringsdict plural variant (e.g. "speakers_live_count") keyed on speakerIds.count. |
| AMENAPP/AMENAPP/DiscoveryOS/DiscoveryPreviewSheet.swift:192 | medium | Event speaker count pluralized by appending "s": "\(count) speaker\(count == 1 ? \"\" : \"s\")". English-only. | Use a .stringsdict plural variant keyed on speakerIds.count. |
| AMENAPP/AMENAPP/SocialGraph/Services/SmartActivityCopyGenerator.swift:48 | medium | Pluralization of "posts" is done by concatenating a count into a hardcoded English plural string. "\(unseenCount) new posts" / "New post" only works for English; other languages have different plural rules. | Move the count→label mapping into a .stringsdict / String Catalog plural entry (e.g. key "new_posts_count" with one/other variants) and load with String(localized:) so each locale supplies its own plural rule. |
| AMENAPP/AMENAPP/SocialGraph/Services/SmartActivityCopyGenerator.swift:58 | medium | Plural suffix built with manual ternary "prayer\(unseenCount > 1 ? \"s\" : \"\")". English-only and breaks for locales with non-binary plural categories. | Replace with a .stringsdict plural variant keyed on the count (e.g. "new_prayers_count") instead of appending "s". |
| AMENAPP/AMENAPP/SpiritualOS/Churches/AmenChurchHubView.swift:1149 | medium | Prayer count text uses a manual ternary to pick "prayer"/"prayers", which only handles English singular/plural and breaks in languages with different plural rules. | Use a String Catalog / .stringsdict plural variant keyed on prayerCount instead of the ternary (requires the plural rule, not just code). |
| AMENAPP/AMENAPP/SpiritualOS/Churches/AmenChurchHubView.swift:1308 | medium | "spot\(...==1 ? "" : "s") left" builds the plural by appending an English "s", which is incorrect for non-English plural rules. | Replace with a .stringsdict plural entry on spotsRemaining (e.g. "%lld spots left") so each locale supplies its own plural forms. |
| AMENAPP/AMENAPP/SpiritualOS/Discovery/AmenChurchHeroCard.swift:31 | medium | memberLabel concatenates a count with a literal 'Members' (and a hardcoded 'K' abbreviation), so it never pluralizes and the number is not locale-grouped. | Move to a .stringsdict / String Catalog plural entry keyed on the member count (e.g. "%lld members") and format the number via .formatted(.number.notation(.compactName)) for the K abbreviation. |
| AMENAPP/AMENAPP/SpiritualOS/LifePlanner/AmenLifePlannerView.swift:200 | medium | Accessibility label pluralizes by manual English suffix: "\(eventCount) event\(eventCount == 1 ? \"\" : \"s\")". This only handles English's one/other rule and breaks for languages with different plural categories (Arab… | Define a stringsdict / String Catalog plural entry keyed on the count (with zero/one/other variants) and look it up via String.localizedStringWithFormat, rather than concatenating "s" in code. |
| AMENAPP/AMENAPP/SpiritualOS/SpacesDashboard/AmenSpacesHeroCardSection.swift:165 | medium | Active prayer count uses manual English ternary pluralization: "Prayer\(activePrayerCount == 1 ? "" : "s")". Only valid for English plural rules. | Use a .stringsdict / String Catalog plural variant keyed on activePrayerCount (e.g. "%lld prayers") instead of appending "s" in code. |
| AMENAPP/AMENAPP/SundayHomeView.swift:333 | medium | Pluralization is done with manual 's' concatenation and a count==1 ternary inside an interpolated string, which is incorrect for languages with non-English plural rules. | Move to a String Catalog / .stringsdict plural entry keyed on the count (e.g. "%lld reflection(s) saved for Monday") so each language supplies its own plural variants. |
| AMENAPP/AMENAPP/UserProfileViewMini.swift:413 | medium | The shared-prayer count label pluralizes via a ternary (prayers == 1 ? "shared prayer" : "shared prayers"). This binary singular/plural rule does not cover languages with more than two plural forms (e.g. Russian, Arabic… | Move the count label to a String Catalog / .stringsdict plural entry keyed on the count (e.g. NSLocalizedString plural variants "%d shared prayer(s)") so the correct plural category is chosen per locale. |
| AMENAPP/AMENAPP/UserProfileViewMini.swift:633 | medium | mutualLabel concatenates a name with "and N others follow them" / "and 1 other follow them". This hardcodes English word order, English subject-verb agreement, and a two-form plural ("other"/"others"). It renders garble… | Replace the manual concatenation with localized format strings containing positional placeholders and a .stringsdict plural for the count, e.g. a localized "%@ and %d others follow them" key with proper plural variants… |
| AMENAPP/ActiveSessionsView.swift:119 | medium | Pluralization done with a ternary appending "s" (device/devices); only handles English plural rules. | Replace with a String Catalog / .stringsdict plural entry keyed on devices.count (one/other and locale-specific categories) using a %lld placeholder. |
| AMENAPP/AmenCreatorWorkspaceView.swift:170 | medium | Scheduled-posts count text uses manual ternary pluralization: "post\(count == 1 ? "" : "s")". This only works for English plural rules and breaks in languages with different plural categories (e.g. Polish, Russian, Arab… | Define a .stringsdict / String Catalog plural entry keyed on the count (e.g. "%lld posts scheduled") and render it with the count, so each locale supplies its own plural variants. |
| AMENAPP/BereanAIAssistantView.swift:1565 | medium | Pluralization via manual "s" suffix: `message\(count == 1 ? "" : "s")`. English-only plural rule; wrong in all other languages, and the whole interpolated phrase is not translatable. | Use a String Catalog / .stringsdict plural entry (key like "%lld messages in session") with one/other variants and look it up with the count argument. |
| AMENAPP/BereanAIAssistantView.swift:7658 | medium | Pluralization via manual "s" suffix: `thread\(count == 1 ? "" : "s")`. English-only; produces incorrect plural forms in other locales and is not translatable as a unit. | Use a String Catalog / .stringsdict plural entry (e.g. "%lld threads") with proper plural variants and pass the count. |
| AMENAPP/BereanDailyFormation/BereanDailyFormationFeedView.swift:125 | medium | Pluralization done by string concatenation with a manual "s" suffix (`card\(count == 1 ? "" : "s")`). This only works for English and produces wrong plural forms in every other language. The whole phrase is also assembl… | Move to a String Catalog / .stringsdict entry with proper plural variants, e.g. a localized key like "%lld cards ready" with one/other (and additional CLDR categories) rules, then `Text("cards_ready \(cards.count)")`. |
| AMENAPP/BereanOS/BereanActionPlanView.swift:274 | medium | Dependency count is rendered as "\(count) dependency" with a fixed singular noun, so it reads '2 dependency', '3 dependency', etc. The noun never pluralizes and the phrase is not localizable. | Use a String Catalog / .stringsdict plural entry on dependsOnIds.count (%lld) providing one/other (and other categories) so it shows '1 dependency' / '2 dependencies' and correct forms per locale. |
| AMENAPP/ChurchExperienceSystem.swift:1033 | medium | Plural built by manual ternary appending 's': "\(count) visit\(count == 1 ? \"\" : \"s\")". Only works for English plural rules. | Replace with a String Catalog / .stringsdict plural variant keyed on the count (e.g. '%lld visit'/'%lld visits') so non-English plural categories render correctly. |
| AMENAPP/ChurchExperienceSystem.swift:1102 | medium | Two manual plurals plus RTL-fragile manual ordering: 'visitor insight'+s and a ' · ' joined 'N would return' clause assembled by left-to-right interpolation. | Use a .stringsdict plural for 'visitor insight(s)' and a single localized format string with positional placeholders for the combined 'N insights · M would return' phrase so plural rules and RTL ordering are handled. |
| AMENAPP/ChurchFitScoreService.swift:174 | medium | User-facing count string "\(church.amenUserCount) AMEN members" concatenates a number with a hardcoded plural noun. Reads "1 AMEN members" and cannot pluralize per-language. | Move to a String Catalog / .stringsdict plural entry, e.g. String(localized: "\(church.amenUserCount) AMEN members") backed by a plural rule (one/other variants). |
| AMENAPP/ConnectForumView.swift:333 | medium | "\(thread.replyCount) Replies" hardcodes the English plural; it renders "1 Replies" and has no plural-rule support for other languages. | Replace with a String Catalog plural entry keyed on `replyCount` (one: "%lld Reply", other: "%lld Replies"). |
| AMENAPP/ConversationThreadView.swift:309 | medium | reply/replies plural chosen with a manual ternary (count == 1 ? "reply" : "replies"). Not translatable and wrong for non-binary plural languages. | Replace with a String Catalog plural variant keyed on count, e.g. Text("^[\(count) reply](inflect: true)") or a .stringsdict entry. |
| AMENAPP/ConversationThreadView.swift:670 | medium | 'View N reply/replies' built with manual ternary plural and concatenation. Not translatable; wrong plural form in many locales. | Use a stringsdict/String Catalog plural format, e.g. a localized "view_replies" key with a plural variant on replies.count. |
| AMENAPP/ConversationThreadView.swift:895 | medium | 'View N more reply/replies in this thread' built with a manual ternary plural and string concatenation. Not localizable and incorrect for non-binary plural languages. | Use a String Catalog/.stringsdict plural-aware format string keyed on (replies.count - visibleLimit). |
| AMENAPP/CreatorView.swift:832 | medium | Count strings concatenate numbers with hardcoded English nouns and the literal middle dot separator: "... followers · ... posts". "followers"/"posts" do not pluralize correctly across languages and cannot vary by count… | Move each count phrase to a String Catalog / .stringsdict plural entry keyed on the count (e.g. "%lld followers", "%lld posts") and compose via a localized format string rather than concatenation. |
| AMENAPP/CrisisHistoryService.swift:234 | medium | Header pluralizes by concatenation: "\(count) visit\(count == 1 ? "" : "s") recorded". English-only singular/plural rule embedded in code. | Use a String Catalog plural entry, e.g. String(localized: "\(service.visits.count) visits recorded") with one/other variants per locale instead of appending "s". |
| AMENAPP/DiscoverSearchUpgrade.swift:181 | medium | Follower count is built with String(format: "%.1fM followers"...) and "\(n) followers" using a hardcoded English "followers" word and a manually-placed suffix. This won't pluralize (1 follower vs N followers) and reorde… | Use a stringsdict / String Catalog plural entry keyed on n (e.g. 'followers_count') with positional %@/%lld, and format the count itself via NumberFormatter (.decimal, locale = .autoupdatingCurrent) so the M/K abbreviat… |
| AMENAPP/EventsView.swift:388 | medium | Event count label pluralizes by appending a literal "s": `"\(filteredEvents.count) event\(count == 1 ? "" : "s")"`. This is English-only and breaks for languages with different plural rules. | Define a String Catalog / .stringsdict plural entry keyed on the count and use it: `String(localized: "\(count) events")` backed by a plural variant. (Requires adding the plural rule, not just a code change.) |
| AMENAPP/FeaturedTestimoniesManager.swift:151 | medium | Count-driven text "Rotates in \(daysUntilRotation) days" hardcodes the plural noun "days"; renders "Rotates in 1 days" and has no plural support for other locales. | Move to a String Catalog / .stringsdict plural entry keyed on the day count (e.g. a localized format with %lld that selects one/other), then interpolate the formatted date separately. |
| AMENAPP/FeedSessionStopScreen.swift:189 | medium | Plural of 'extension(s)' is built by manual concatenation with a ternary appending an 's'. This breaks in languages with non-binary plural rules (Arabic, Polish, Russian) and is not translatable. | Use a String Catalog / .stringsdict plural entry, e.g. Text("^[\(remaining) extension](inflect: true) remaining today") or a localized format key with a plural variant keyed on the count. |
| AMENAPP/HeyFeedActiveRequestsView.swift:272 | medium | Resonance count label is concatenated with a hardcoded English plural noun ("resonances"), so it reads "1 resonances" and cannot pluralize per-language. | Use a String Catalog / .stringsdict plural variant keyed on resonanceCount with one/other forms instead of concatenation. |
| AMENAPP/JobModels.swift:188 | medium | responseTimeLabel concatenates a count with a hardcoded plural noun: "Responds within \(days) days". Cannot pluralize correctly (e.g. "Responds within 1 days" edge cases, and other languages need distinct plural forms). | Use a .stringsdict / String Catalog plural variant keyed on `days` (one/other), surfaced via String(localized:). Keep the day/week branches as separate localized strings. |
| AMENAPP/JobRecruiterDashboardView.swift:176 | medium | Count text '\(apps.count) applicants' (and '\(unread) new' on line 168) concatenates the number with a fixed English noun, so it never agrees with the count or with locale plural rules. | Move the count strings to a String Catalog / .stringsdict plural entry (e.g. key 'applicants_count' with one/other variants) and render with the localized count format. Requires defining the plural rule, not just code. |
| AMENAPP/LiveActivityManager.swift:373 | medium | serviceDisplayTime builds user-visible Live Activity countdown text by concatenating the minute count with a fixed 'min' abbreviation in fixed order ('Starts in N min' / 'N min in'), with no pluralization and a non-tran… | Use localized format strings with positional placeholders and a .stringsdict plural for the minute unit (e.g. "Starts in %lld minutes") so wording, ordering, and plurals adapt per locale. |
| AMENAPP/MentorProfileView.swift:240 | medium | Session count pluralization is done with a manual ternary appending 's' ('session' vs 'sessions'). This English-only rule produces wrong grammar in other languages and is invisible to the translation system. | Replace with a String Catalog / .stringsdict plural entry keyed on plan.sessionsPerMonth (%lld) so each locale supplies its own plural forms. |
| AMENAPP/NotificationDigestView.swift:341 | medium | The 'more items' counter is built with concatenation "+ \(count) more", which has no plural handling (always English 'more', no singular/plural agreement) and a leading '+' that assumes LTR. | Use a String Catalog / .stringsdict plural entry keyed on the count, e.g. Text(String(localized: "digest.moreItems-\(item.deepLinks.count - item.preview.count)")) with one/other variants supplied per language (requires… |
| AMENAPP/PeopleDiscoveryView.swift:911 | medium | "\(count) posts" (and the "M posts"/"K posts" branches above) hardcode the English plural "posts" with no singular form and no plural-rule support. "1 posts" is wrong in English and entirely wrong in languages with mult… | Define a `.stringsdict`/String Catalog plural entry keyed on the count, e.g. `Text("posts_count", comment: ...)` with one/other variants, and pass `count`. The compact (K/M) branch should use a separate localized format… |
| AMENAPP/ResourcesView.swift:319 | medium | Result count text is pluralized in code with a ternary appending 's': `"\(count) result\(count == 1 ? "" : "s")"`. This English-only rule breaks for languages with different plural categories (e.g. Polish, Russian, Arab… | Move to a String Catalog / .stringsdict plural entry, e.g. a key "resultsCount" with %lld and per-language plural variants (one/other/...), then `Text(String(localized: "resultsCount", ...))` or a NSLocalizedString form… |
| AMENAPP/SafetyUIComponents.swift:462 | medium | The account-status violation count is built with "\(count) \(count == 1 ? "violation" : "violations") in past 30 days" using a ternary plural and concatenation. Binary plural rule and English word order do not localize… | Use a .stringsdict / String Catalog plural entry for the violations count (e.g. "%d violation(s) in past 30 days") so the locale picks the right plural form and translation. |
| AMENAPP/SafetyUIComponents.swift:512 | medium | The pending-appeals row builds "\(count) \(count == 1 ? "appeal" : "appeals") under review" with a ternary plural and concatenation, which does not localize plural categories or word order. | Move to a .stringsdict / String Catalog plural variant keyed on count (e.g. "%d appeal(s) under review"). |
| AMENAPP/SmartNotificationService.swift:332 | medium | Batched notification titles/bodies hardcode English plurals like "\(count) People Prayed" / "\(count) people prayed for your request" (and similarly for amens, comments, follows, reposts, mentions). These are user-facin… | Localize via String Catalog/.stringsdict plural variants keyed on `count` for each batch type, replacing the manual count==1 branch. |
| AMENAPP/SuggestedAccountCard.swift:143 | medium | Follower count label is built by concatenation with a hardcoded English plural noun ("followers"), so it never reads "1 follower" and cannot pluralize correctly in other languages. | Use a String Catalog / .stringsdict plural entry keyed on the count (e.g. a localized "%lld followers" with one/other variants) rather than string concatenation. |
| AMENAPP/WorkflowConfigView.swift:343 | medium | Step delay is built by concatenation: "after \(delay)m" / "\(delay / 60)h" with hardcoded English 'after' prefix and m/h units. No pluralization and the 'after' word is not localizable; prefix ordering breaks in RTL. | Use a localized format string with a positional placeholder for the duration and format the duration via Measurement/DateComponentsFormatter or Duration.formatted(.units(...)) so units and word order are locale-aware. |
| AMENAPP/AIBibleStudyView.swift:1107 | low | Usage banner 'X of Y free messages remaining today' is count-driven but built by interpolation with a fixed plural 'messages'; will read wrong for languages with singular/other forms (e.g. '1 of 5 free messages'). | Move into a String Catalog / .stringsdict plural entry keyed on messagesRemaining so the noun pluralizes per the locale's rules. |
| AMENAPP/AMENAPP/AMENAPP/Covenant/AmenCreatorHubView.swift:613 | low | HubDigestCard hardcodes an English count string "3 posts · 2 prayer updates · 1 event" with literal plural nouns and "·" separators. This is non-localizable and uses fixed plural forms (and "1 event" singular hardcoded). | Build each segment from localized plural strings (.stringsdict) and join with a localized separator, or replace with real counts formatted via plural-aware catalog entries. |
| AMENAPP/AMENAPP/AMENAPP/ObjectHub/AmenMentorChannelView.swift:462 | low | Study-series lessons label is built by concatenating "\(series.lessonCount) lessons" with a hardcoded plural "lessons" (also at lines 499 and 1499). No singular/plural handling and English-only word; breaks for count==1… | Use a .stringsdict / String Catalog plural entry keyed on lessonCount (e.g. "%lld lessons") with proper one/other variants, applied at all three sites. |
| AMENAPP/AMENAPP/AMENActivityIntelligenceEngine.swift:370 | low | buildSubtitle composes count fragments by concatenation ("\(prayed) prayed", "\(amens) encouraged", "\(comments) commented") joined with " • ". These are count-driven English strings with no plural handling and fixed or… | Localize each fragment via a .stringsdict plural entry keyed on the count, and build the joined string with a localized separator; let the catalog handle plural form per locale. |
| AMENAPP/AMENAPP/AdaptiveComposer/Cards/AttachmentCardsA.swift:687 | low | Visible vote count uses manual "s" plural: "\(payload.totalVotes) vote\(payload.totalVotes == 1 ? \"\" : \"s\")". English-only pluralization. | Use a .stringsdict / String Catalog plural variant for "%lld vote(s)". |
| AMENAPP/AMENAPP/AdaptiveComposer/Cards/AttachmentCardsC.swift:164 | low | Manual English pluralization in an accessibility hint: "\(slotsRemaining) slot\(slotsRemaining == 1 ? \"\" : \"s\")". The +"s" rule is English-only and breaks in languages with different plural categories. | Use a .stringsdict / String Catalog plural entry keyed on the count (e.g. NSLocalizedString plural rule for "%lld slot(s) remaining") rather than concatenating "s". |
| AMENAPP/AMENAPP/AdaptiveComposer/Cards/AttachmentCardsC.swift:915 | low | Visible passage count uses manual "s" plural: "\(payload.passages.count) passage\(payload.passages.count == 1 ? \"\" : \"s\")". | Replace with a .stringsdict / String Catalog plural variant for "%lld passage(s)". |
| AMENAPP/AMENAPP/AdaptiveComposer/Cards/AttachmentCardsC.swift:1012 | low | Visible post count uses manual "s" plural: "\(payload.postCount) post\(payload.postCount == 1 ? \"\" : \"s\")". | Replace with a .stringsdict / String Catalog plural variant for "%lld post(s)". |
| AMENAPP/AMENAPP/Catalog/FollowKnowledgeView.swift:321 | low | The followed-topic 'new works' badge is built as "\(recentWorkCount) new" — a count concatenated with a fixed word, with no plural handling. Many locales require different forms for 1 vs many, and the noun is dropped en… | Use a String Catalog / .stringsdict plural entry keyed on the count, e.g. Text(String.localizedStringWithFormat(NSLocalizedString("%d new", comment: "new works count"), topic.recentWorkCount)) with the plural rule defin… |
| AMENAPP/AMENAPP/CommunityOS/Church/ChurchObjectHub.swift:504 | low | Duration label "\(minutes) min" hardcodes the English abbreviation with no plural handling and no locale-aware unit formatting. | Use Measurement formatting: Measurement(value: Double(sermon.durationSeconds/60), unit: UnitDuration.minutes).formatted(), or a .stringsdict plural key for the "min" unit. |
| AMENAPP/AMENAPP/CommunityOS/Prayer/PrayerRoomView.swift:66 | low | Relative-time label built with concatenation: '\(Int(diff/60))m ago' / '\(n)h ago' plus 'just now'. The 'm ago'/'h ago' units and word order are hardcoded English and not pluralized/localized. | Use RelativeDateTimeFormatter (locale-aware, handles units/plurals/RTL): let f = RelativeDateTimeFormatter(); f.localizedString(for: createdAt, relativeTo: Date()). |
| AMENAPP/AMENAPP/ConnectSpaces/Events/AmenEventBroadcastView.swift:457 | low | The success toast pluralizes with manual concatenation "member\(count == 1 ? "" : "s")", which only covers English plural rules. | Use a String Catalog / .stringsdict plural variant keyed on count (e.g. "Message sent to %lld members") so other locales get correct plural forms. |
| AMENAPP/AMENAPP/ConnectSpaces/Events/AmenSmartEventComposerView.swift:119 | low | durationLabel builds the hour count with manual pluralization "hour\(h > 1 ? "s" : "")", which only handles English singular/plural and is wrong in languages with other plural rules. | Move the duration string into a .stringsdict / String Catalog plural entry keyed on the hour count (and minutes), rather than concatenating "s". |
| AMENAPP/AMENAPP/CreatorProfiles/EventCard.swift:144 | low | Travel-time label hardcodes the English noun and never varies for plural: `"· \(travel) min travel"`. While 'min' is an abbreviation, the surrounding phrase is not localizable and will not translate or pluralize. | Use a localized format string with a positional placeholder (and a plural variant if 'minute(s)' is ever spelled out), e.g. a String Catalog entry like "%lld min travel" translated per language, rather than literal inte… |
| AMENAPP/AMENAPP/CreatorProfiles/FeaturedSmartModuleCard.swift:155 | low | "\(openRequests) prayer requests" hardcodes the English plural; "1 prayer requests" is grammatically wrong and unlocalizable. | Use a String Catalog plural variant keyed on `openRequests` (one: "%lld prayer request", other: "%lld prayer requests"). |
| AMENAPP/AMENAPP/CreatorProfiles/UnifiedTimelineView.swift:115 | low | Count-driven labels are built by interpolation with hardcoded English nouns: "\(p.prayedCount) praying" and "\(c.modules.count) modules" (line 116). Not localizable and no plural handling for languages that distinguish… | Replace each with a String Catalog plural entry (e.g. "praying-count" / "modules-count" with %lld and one/other variants) and look them up with the count argument, instead of literal string interpolation. |
| AMENAPP/AdaptiveGlassInboxView.swift:1260 | low | Hero summary "\(unread) unread" — count-driven text with no plural handling (English-only and not even pluralized). | Use a .stringsdict plural entry keyed on unread (e.g. "unread_count") so each locale supplies the right form. |
| AMENAPP/AmenConnectV2View.swift:567 | low | Tile subtitle builds a plural by interpolation: "\(viewModel.spaces.count) workspaces" with a fixed plural noun; the count of 1 still renders "1 workspaces" and other locales need distinct plural forms. | Use a String Catalog plural variant keyed on spaces.count (one/other) instead of the hardcoded "workspaces" suffix. |
| AMENAPP/AmenHealthyImmersiveMediaSystem.swift:181 | low | progressLabel "\(...) of \(itemIds.count)" hardcodes the English connective word "of" between two counts; this user-facing position label is not translatable and assumes LTR word order. | Use a localized format string with positional placeholders, e.g. String(localized: "\(current) of \(total)") in a String Catalog so the connector and order can be localized (and mirror under RTL). |
| AMENAPP/BereanDailyFormation/BereanFormationCardViews.swift:152 | low | Strength-bar label '\(pct)% complete · \(remaining) days remaining' uses a bare 'days' with no singular form and concatenates fields LTR. | Use a .stringsdict plural for 'N days remaining' and a localized format string joining the two clauses. |
| AMENAPP/ChurchChemistryService.swift:180 | low | Count phrase uses a manual ternary plural and English word order: "\(count) person/people you may know attend here". This only handles English singular/plural and cannot express other languages' plural rules. | Replace the ternary with a String Catalog / .stringsdict plural entry keyed on mutualCount for the whole sentence (e.g. "%lld people you may know attend here"). |
| AMENAPP/ChurchJourneyTimelineView.swift:43 | low | 'N milestones' is always pluralized with a hardcoded English 's' and never switches to singular for 1; also not translatable. | Use a String Catalog/.stringsdict plural variant keyed on milestones.count for 'milestone(s)'. |
| AMENAPP/ChurchNotesEditor.swift:710 | low | "\(wordCount) words" (line 710) and "\(characterCount) characters" (line 716) concatenate counts with hardcoded English plural nouns; no singular form, no locale plural support. The milestone label "\(words) words ✦" (l… | Replace with String Catalog / .stringsdict plural entries keyed on the count (one/other variants) for words, characters, and the milestone label. |
| AMENAPP/ChurchNotesPDFExporter.swift:161 | low | "Note \(index + 1) of \(notes.count)" and "\(notes.count) Notes" (line 219) build count-driven, ordered English phrases by interpolation; both the word order and the plural "Notes" will not localize. | Use a localized format string with positional placeholders for "Note %1$d of %2$d" and a .stringsdict plural for the "%d Notes" cover label so both word order and plural form follow the target locale. |
| AMENAPP/CommentSafetySystem.swift:701 | low | Cooldown message embeds a count with a hardcoded English unit and no plural handling: "take a \(cooldownSeconds) second break" — reads "1 second"/"300 second" and is not localizable as a plural. | Use a .stringsdict plural entry keyed on the seconds count (e.g. "... take a %lld second break") so singular/plural and other languages' rules are handled. |
| AMENAPP/CreatorOS/CreatorMonetizationView.swift:134 | low | Subscriber count text "From N active subscribers" hardcodes the English plural noun with no singular form (would read 'From 1 active subscribers'). | Move to a .stringsdict / String Catalog plural entry keyed on the count so singular/plural and other languages' plural rules are handled. |
| AMENAPP/CreatorOS/CreatorMonetizationView.swift:170 | low | "N subscribers" built by interpolation with a hardcoded English plural 's', no singular handling. | Use a .stringsdict plural variant for the subscriber count instead of a bare interpolated noun. |
| AMENAPP/CreatorView.swift:927 | low | Duration label "\(post.durationMinutes) min" hardcodes an English-only abbreviation with no plural handling. | Use Duration/Measurement formatting or a localized plural string, e.g. Duration.seconds(post.durationMinutes*60).formatted(.units(allowed: [.minutes])) or a .stringsdict entry for "%lld min". |
| AMENAPP/CreatorView.swift:1021 | low | "\(series.episodeCount) episodes" concatenates a count with a hardcoded English plural noun; will read "1 episodes" and cannot localize plural rules. | Use a .stringsdict / String Catalog plural variant keyed on episodeCount (e.g. "%lld episodes"). |
| AMENAPP/CrisisHistoryService.swift:33 | low | formattedDuration returns user-facing "< 1 min" / "\(mins) min" with a hardcoded, never-pluralized English "min" abbreviation used in the exported/displayed list. | Use Measurement<UnitDuration> formatted with .current locale, e.g. Measurement(value: Double(mins), unit: UnitDuration.minutes).formatted(), or a String Catalog plural for the minute unit. |
| AMENAPP/Date+Extensions.swift:23 | low | Relative time strings ('Just now', 'Nm ago', 'Nh ago', etc.) are hardcoded English with no localization and no plural handling. Visible throughout the app on posts/comments. | Use RelativeDateTimeFormatter (formatter.localizedString(for:relativeTo:)) which is locale-aware and handles plurals, or back each unit with a .stringsdict plural key. |
| AMENAPP/EnhancedQuietHoursView.swift:727 | low | Suggestion row '\(suggestion.dataPoints) samples' uses a hardcoded English plural noun with no singular form. | Use a .stringsdict plural variant keyed on dataPoints. |
| AMENAPP/EventsView.swift:680 | low | "\(event.rsvpCount) people going" is a hardcoded English plural noun; it reads wrong for count == 1 and has no localized plural variants. | Use a stringsdict/String Catalog plural variant on rsvpCount (e.g. "%lld going" with one/other forms). |
| AMENAPP/FindChurchOS/FindChurch2CommunitySignalsView.swift:169 | low | Signal label '\(formatted) AMEN member(s) attend' uses a code-only ternary (count == 1 ? "" : "s") for pluralization, which only works for English plural rules. | Use a .stringsdict / String Catalog plural keyed on amenMemberCount; languages with more than two plural forms cannot be handled by the ternary. |
| AMENAPP/FindChurchOS/FindChurch2CommunitySignalsView.swift:176 | low | '\(friendSavedCount) friend(s) saved this' pluralizes via an English-only ternary on the count. | Move to a .stringsdict plural variant keyed on friendSavedCount. |
| AMENAPP/FindChurchOS/FindChurch2CommunitySignalsView.swift:98 | low | Avatar-cluster overflow '+\(overflowCount) more' is a hardcoded English phrase with leading '+' concatenated to a number; not translatable and the leading '+' ordering is LTR-fixed. | Use a localized format string with a positional count placeholder, e.g. String(format: NSLocalizedString("+%lld more", comment: "overflow count"), overflowCount), ideally via .stringsdict. |
| AMENAPP/FindChurchOS/FindChurch2SmartChurchCard.swift:83 | low | GatheringCountBadge '\(count) gathering(s)' uses an English-only ternary for the plural suffix. | Use a .stringsdict plural variant keyed on count for the gathering noun. |
| AMENAPP/FollowerAvatarStack.swift:135 | low | 'N followers' uses a hardcoded English 'followers' with no singular form and no localization. | Use a String Catalog/.stringsdict plural variant for 'follower(s)' keyed on followerCount (note: with compact formatting, gate the plural on the raw count). |
| AMENAPP/FollowersAnalyticsView.swift:413 | low | 'N followers' on the top-follower row is hardcoded English with no singular/plural rule and no localization. | Use a String Catalog/.stringsdict plural variant for 'follower(s)' keyed on user.followersCount. |
| AMENAPP/MusicContentLayer/ListeningDiscussionRoomView.swift:93 | low | Room-end system message '— Room ended with N participants.' hardcodes the English plural noun with no singular variant. | Use a .stringsdict plural keyed on participants.count for the 'N participants' fragment within a localized sentence. |
| AMENAPP/MusicContentLayer/ListeningDiscussionRoomView.swift:239 | low | Poll option accessibility label '\(option), \(votes) votes' uses a bare English 'votes' with no singular form. | Use a .stringsdict plural for the vote count. |
| AMENAPP/PeopleDiscoveryView.swift:1496 | low | "\(count) Amens" concatenates a count with a hardcoded English plural noun. Singular ("1 Amens") is wrong, and other languages need proper plural categories. | Use a String Catalog plural variant keyed on `count` (one: "%lld Amen", other: "%lld Amens") rather than string interpolation. |
| AMENAPP/PushNotificationManager.swift:912 | low | OTP retry message concatenates a count with a hardcoded plural noun: "Invalid code. \(5 - otpAttempts) attempts remaining." Shows "1 attempts remaining" and is not pluralizable per-language. | Use a String Catalog / .stringsdict plural variant for the remaining-attempts count (one/other), surfaced through String(localized:). |
| AMENAPP/SearchService.swift:483 | low | Trending item subtitle "\(count) posts" hardcodes the English plural noun for the user-visible trending list. | Use a String Catalog/.stringsdict plural variant on count (one/other) for the "posts" label. |
| AMENAPP/SermonRecorder.swift:309 | low | "\(recorder.chunkCount) chunks" concatenates the count with a hardcoded English plural noun; no singular form and no locale plural rules. | Use a .stringsdict / String Catalog plural variant keyed on chunkCount (one/other), e.g. Text("chunks_count", count: recorder.chunkCount). |
| AMENAPP/SuggestedAccountPeekSheet.swift:238 | low | Mutual-connection count label uses manual English plural: "\(totalCount) mutual connection\(totalCount == 1 ? \"\" : \"s\")". English-only "s" rule. | Use a .stringsdict / String Catalog plural variant for "%lld mutual connection(s)". |
| AMENAPP/VergeCreatorStudioView.swift:316 | low | "\(room.participantCount) attendees" hardcodes the English plural "attendees" with no singular form and no plural-rule support for other languages. | Move to a String Catalog / .stringsdict plural entry keyed on the count (e.g. "%lld attendees" with one/other variants), rendered via Text("attendees_count") with the count argument. |
| AMENAPP/WalkWithChristView.swift:1173 | low | Module duration label "\(module.estimatedMinutes) min" interpolates a count with a fixed English unit and no plural form. Not localizable. | Use a localized measurement/plural format, e.g. `Measurement(value: Double(module.estimatedMinutes), unit: UnitDuration.minutes).formatted()` or a String Catalog plural key "%lld min" with per-language variants. |

## 4. Layout (directional)
| File:Line | Severity | Issue | Proposed fix |
|---|---|---|---|
| AMENAPP/AMENAPP/AMENAPP/ObjectHub/AmenMentorChannelView.swift:1035 | medium | The floating back button uses a fixed "chevron.left" SF Symbol. Back affordances should mirror under RTL; a hardcoded left-chevron points the wrong way (it should point right / trailing) in Arabic/Hebrew. | Use a direction-aware symbol such as "chevron.backward" (which mirrors automatically), or apply `.flipsForRightToLeftLayoutDirection(true)` to the chevron. |

## 5. gdpr.* broken keys (consent dialog shows raw key strings today)

`GDPRConsentView.swift` calls `NSLocalizedString` for keys that have **no English value** anywhere (base `en.lproj/Localizable.strings` lacks them; `es.lproj` has self-referential placeholders like `"gdpr.title" = "gdpr.title"`). A user's first prayer/testimony submission currently shows raw keys in a GDPR Article 9 consent dialog.

| Key | Used at | State | Proposed English (review before applying) |
|---|---|---|---|
| `gdpr.title` | GDPRConsentView.swift:45 | placeholder = key | Protecting Your Sensitive Data |
| `gdpr.body.prayer` | GDPRConsentView.swift:50 | missing entirely | Prayer requests can reveal deeply personal beliefs. We treat them as sensitive religious data and store them only with your explicit consent. |
| `gdpr.body.testimony` | GDPRConsentView.swift:50 | missing entirely | Testimonies can reveal deeply personal beliefs. We treat them as sensitive religious data and store them only with your explicit consent. |
| `gdpr.bullet.encrypted` | :58 | placeholder = key | Encrypted in transit and at rest |
| `gdpr.bullet.visibility` | :59 | placeholder = key | Visible only to the audience you choose |
| `gdpr.bullet.deletion` | :60 | placeholder = key | Deleted permanently whenever you remove it |
| `gdpr.bullet.neverSold` | :61 | placeholder = key | Never sold or shared with advertisers |
| `gdpr.agree` | :79 | placeholder = key | I Agree |
| `gdpr.decline` | :91 | placeholder = key | Not Now |

**Destination flag:** all are `NSLocalizedString` keys. If fixed BEFORE the String Catalog migration → add English values to `AMENAPP/en.lproj/Localizable.strings` (legacy base). If fixed AFTER migration → they auto-extract into `Localizable.xcstrings` on build; set the English value there (recommended — one source of truth). Also: `GDPRConsentView.swift:65` ("By tapping \"I Agree\"…") is a hardcoded English literal, NOT an NSLocalizedString — key it too or it never localizes.
