{
  "name": "amen-firestore-rules-tests",
  "private": true,
  "description": "Firestore Security Rules unit tests using the Firebase Emulator Suite",
  "scripts": {
    "test": "jest --forceExit",
    "test:watch": "jest --watch"
  },
  "engines": {
    "node": "22"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/jest": "^29.5.14",
    "firebase-admin": "^12.2.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.9",
    "typescript": "^5.4.5"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testTimeout": 30000,
    "globalSetup": "./jest.globalSetup.ts",
    "globalTeardown": "./jest.globalTeardown.ts"
  }
}
