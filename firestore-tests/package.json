{
  "name": "amen-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Emulator rules tests (NG-1 posts read gate, etc.)",
  "scripts": {
    "test:ng1": "firebase emulators:exec --only firestore \"node ng1-posts-read.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase": "^10.0.0"
  }
}
