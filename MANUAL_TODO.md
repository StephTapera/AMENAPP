# AMEN — Manual To-Do (things only a human can do)
_Last updated: 2026-05-27. Generated by full-app audit (v1–v4 + convergence pass). Build: 0 errors._

---

## 🚨 BLOCKERS — must resolve before App Store submission

- [ ] **GIVING / IAP compliance decision** — Decide merchant-of-record + nonprofit status BEFORE building more of Giving. In-app Stripe collection by a non-Apple-approved nonprofit = Guideline 3.2.1 rejection. Choose **(A)** Apple Nonprofit Program + Apple Pay (every listed church also needs approval), OR **(B)** move Giving out-of-app (SFSafariViewController → church giving page). Cannot be coded around.
  - **WHERE**: Business/legal decision → App Store Connect nonprofit enrollment → architecture change (HYBRID: agent re-architects code once decided).

- [ ] **Sign in with Apple** — Required when any third-party login (Google) is offered (Guideline 4.8). Enable capability in Developer portal → add to App ID → Xcode Signing & Capabilities.
  - **WHERE**: Apple Developer portal + Xcode. (HYBRID: agent adds the SwiftUI button + AuthenticationServices code once capability is enabled.)

- [ ] **Age-rating questionnaire** — New Apple 13+/16+/18+ tiers effective July 2025. Deadline was January 31, 2026. Submit updated questionnaire in App Store Connect immediately.
  - **WHERE**: App Store Connect → Your App → Age Rating.

- [ ] **UGC 24-hour moderation SLA** — Apple Guideline 1.2: must remove content AND eject offending user within 24 hours. A report button that files into a void is not compliant. Set up an active moderation queue you watch daily.
  - **WHERE**: Firebase console email alias or a moderation tool (Hive / Perspective / admin dashboard). (HYBRID: Report/Block/Mute UI + Cloud Functions exist in code; human must operate the queue.)

- [ ] **EULA / Terms / Privacy Policy content** — Must explicitly prohibit objectionable content. Privacy Policy must describe all data collected (including AI, HealthKit, voice, location). Legal review recommended.
  - **WHERE**: Work with a lawyer; host on your domain; agent wires the signup gate + in-app links (HYBRID).

- [ ] **APNs production cert/key** — Upload the production APNs key (not dev key) to Firebase Cloud Messaging. Dev push ≠ prod push — silent failure in production.
  - **WHERE**: Firebase Console → Project Settings → Cloud Messaging → iOS App Configuration.

- [ ] **In-app account deletion — end-to-end test** — Agent audit confirms the deletion code covers Auth + Firestore + Storage + RTDB + Algolia + Stripe. Must be verified on a real account before submission (App Store Guideline 5.1.1v).
  - **WHERE**: Test the full flow: Settings → Account → Delete Account → enter "DELETE" → confirm Auth deleted, Firestore wiped, Storage removed, email no longer in Algolia.

---

## 🍎 Apple / App Store Connect

- [ ] **App Privacy "nutrition label" — sync with PrivacyInfo.xcprivacy** — Agent updated PrivacyInfo.xcprivacy to add AudioData (voice recordings) and Health (HealthKit) and set NSPrivacyTracking=false. You must update the matching App Privacy section in App Store Connect to stay in sync.
  - **WHERE**: App Store Connect → Your App → App Privacy → add AudioData and Health entries.

- [ ] **App Store assets** — Current screenshots (updated UI after all audit fixes), description, keywords, support URL, marketing URL, export-compliance answer.
  - **WHERE**: App Store Connect → App Information / Screenshots.

- [ ] **Demo/test account credentials** — Required for UGC apps: App Review notes must include a working account showing posts, comments, reports. Reviewers cannot test moderation flows without it.
  - **WHERE**: App Store Connect → Your App → App Review Information.

- [ ] **Provisioning profiles / signing / capabilities** — Verify production profiles cover all capabilities: Push, Sign in with Apple, Associated Domains, HealthKit, etc.
  - **WHERE**: Xcode → Signing & Capabilities + Apple Developer portal.

- [ ] **App icon + launch screen** — Verify all required AppIcon sizes in Assets.xcassets. Confirm launch screen matches current branding.
  - **WHERE**: Assets.xcassets → AppIcon; LaunchScreen.storyboard or Info.plist.

- [ ] **Version / build number** — Bump CFBundleShortVersionString and CFBundleVersion before submission.
  - **WHERE**: Xcode → Project settings → Version / Build, or Xcode Settings target.

- [ ] **External TestFlight testers** — Strongly recommended before public launch: diverse devices + iOS versions.
  - **WHERE**: App Store Connect → TestFlight.

---

## 💳 Giving / Payments

_(See BLOCKER above for the architectural decision. These apply once the path is chosen.)_

- [ ] **Data export backend CF** — The "Download My Data" button in Settings now shows a toast (agent fix), but the actual export Cloud Function must be built and deployed. It needs to collect: posts, comments, likes, notes, Berean history, profile data, and deliver a download link by email.
  - **WHERE**: `Backend/functions/src/` → new `requestDataExport` callable; triggered by the Settings flow. See `AMENSettingsSystem.swift` line 1998 for the client-side hook.

- [ ] **Stripe dashboard** — Set up production webhooks + idempotency keys. (HYBRID: agent writes the CF webhook handler; you configure the endpoint URL in Stripe dashboard → Developers → Webhooks.)

- [ ] **Verify Giving is not publicly reachable** until the compliance decision is implemented.

---

## 🛡 UGC Safety

- [ ] **Operate the moderation queue daily** — Report/Block/Mute UI is wired + Cloud Functions exist. Human must actively monitor it (24-hour Apple SLA).
- [ ] **Publish developer contact info** reachable in-app (Guideline 1.2) — email or support link.
- [ ] **Provide demo account** to App Review showing visible UGC (posts, comments, prayer requests, reports).

---

## 🤖 AI System Decisions

- [ ] **generateDailyVerse — tier gate decision** — Agent added `AI_PER_DAY` rate limit. Decide: free or paid-only? If paid-only, add `getBereanEntitlement()` check in `Backend/functions/src/generateDailyVerse.ts` then redeploy.
  - **WHERE**: Product decision → edit `generateDailyVerse.ts` line ~52 → `firebase deploy --only functions:generateDailyVerse`.

- [ ] **evaluateTone — real AI call or keep stub?** — Currently uses a local heuristic (no Claude call). If real AI call is added later, MUST add `getBereanEntitlement()` tier gate + `enforceRateLimit` before going live or it becomes a cost-leakage vector. No rate limit or tier gate exists today.
  - **WHERE**: `Backend/functions/src/aiUsageLabels.ts` lines 188–213.

- [ ] **bereanChatProxyStream auth enforcement** — HTTP `onRequest` function doesn't have App Check auto-enforcement. Verify it manually checks `Authorization` header or Firebase ID token before processing. If not, add manual token verification.
  - **WHERE**: `Backend/functions/src/bereanChatProxy.ts` or similar streaming function.

- [ ] **Crisis CF audit trail** — Client-side crisis detection correctly defers to a Cloud Function. Verify that function actually writes an audit record (user_id, timestamp, crisis_type, response_shown) to a server-only `crisisAuditLog` Firestore collection. Without this, there's no legal record that the app responded to a crisis report.
  - **WHERE**: Firebase Functions → the CF reading from `crisisDetectionRequests`.

- [ ] **Shadow-ban UX transparency decision** — Audit confirmed the shadow-ban system blocks content CREATION (after 5 violations in 30 days), not visibility. This is defensible moderation. However, you should decide whether to notify the user their account has moderation restrictions (transparent, better UX) vs. stay silent (current behavior). FTC guidance recommends transparency.
  - **WHERE**: `AMENAPP/AdvancedModerationService.swift` lines 431–526.

---

## 🔑 Dashboards & Credentials

- [ ] **Rotate previously exposed API keys** — YOUVERSION_API_KEY, CLAUDE_API_KEY, OPENAI_API_KEY, and XAI_KEY were committed to git history before being removed. Anyone with repo access can extract them. Rotate immediately in each dashboard.
  - **WHERE**: Anthropic console → API keys; OpenAI platform → API keys; YouVersion developer portal; xAI dashboard. Then `git filter-repo` or contact GitHub support to purge history if repo is public.

- [ ] **Google Cloud Console: Firebase API key restrictions** — `AIzaSyBR...` (in GoogleService-Info.plist) is public by design but must be restricted. Verify it's restricted to iOS bundle ID `tapera.AMENAPP` and only required Firebase services.
  - **WHERE**: Google Cloud Console (project `amen-5e359`) → APIs & Services → Credentials.

- [ ] **Crashlytics dSYM symbolication** — Confirm release builds upload dSYMs automatically. Without this, crash reports are unreadable hex addresses.
  - **WHERE**: Firebase Console → Crashlytics. Add Upload dSYMs build phase if missing: Product → Scheme → Edit Scheme → Build → Post-actions.

- [ ] **Firebase billing alerts** — Set a monthly budget alert in Google Cloud Billing to catch Firestore/Cloud Functions cost spikes before they become a surprise invoice.
  - **WHERE**: Google Cloud Console → Billing → Budgets & Alerts.

- [ ] **Firestore scheduled backups** — Enable scheduled exports to Cloud Storage so a bad migration is recoverable.
  - **WHERE**: Firebase Console → Firestore → Backups, or `gcloud firestore export`.

- [ ] **Algolia / Pinecone dashboards** — Verify API rate limits and index configuration match production load. (HYBRID: agent writes sync triggers; human configures the dashboard.)

---

## ☁️ Cloud Functions — deploy checklist

Run `firebase deploy --only functions` after making any backend changes. Functions that MUST be deployed before launch:

| Function | Reason |
|---|---|
| `generateDailyVerse` | Modified in commit 5c172a8: added `AI_PER_DAY` rate limit |
| `cfCommentCount` | Fixed in audit v2 |
| `cfInvalidationPagination` | Fixed in audit v2 |

Functions that need a code change THEN deploy:
| Function | What to add |
|---|---|
| `evaluateTone` | Rate limit if/when a real AI call is added |
| `bereanChatProxyStream` | Manual auth token verification |
| `generateDailyVerse` | Tier gate (if product decides paid-only) |
| `requestDataExport` | Build this new function for the data export feature |

---

## ✅ Content & External Facts to Verify

- [ ] **Crisis resources — re-verify all numbers before launch** and set a quarterly calendar reminder to re-check:
  - **US**: 988 Suicide & Crisis Lifeline (call/text) ✅ in code
  - **US**: 741741 Crisis Text Line "HOME" ✅ in code
  - **Canada**: 686868 Kids Help Phone "HELLO" ✅ in code (correct keyword)
  - **NEDA**: NOT referenced (correctly removed — helpline shut down May 2023) ✅
  - **Verify**: Any regional numbers for Australia, NZ, UK if international users are supported

- [ ] **"Like = lightbulb" icon on posts** — Confirm the lightbulb is the final intended design (not a placeholder). If intentional, the accessibility label "Add lightbulb reaction" is now in place.
  - **WHERE**: `AMENAPP/PostCard.swift`.

- [ ] **Translation feature smoke test** — Verify "See translation" on posts calls a translation API and returns real translated text (not garbled seed data).
  - **WHERE**: `AIMessagingComponents.swift` + `MeaningAwareTranslationService.swift`.

- [ ] **Parental consent (COPPA under-13)** — Age gate blocks <13 from proceeding. Confirm this is enforced server-side (Firestore rule + auth claim), not just client-side UI.
  - **WHERE**: Auth service + Firestore rules. Minors who bypass client gate must be blocked server-side.

---

## 🌐 Infrastructure

- [ ] **Universal links / apple-app-site-association** — Host the AASA file on your domain (`https://yourdomain.com/.well-known/apple-app-site-association`). Verify Associated Domains entitlement in Xcode matches. Test that share links from cold start open the app.
  - **WHERE**: Your web server + Xcode → Signing & Capabilities → Associated Domains.

---

## 📱 Real-Device Testing

- [ ] **Physical device test (older device, e.g. iPhone SE)** — Camera, Face ID/Touch ID, push notification delivery, location accuracy, scroll performance on a long feed, memory under a long session.
- [ ] **Reinstall test** — Delete app → reinstall → confirm: last email pre-filled, NOT auto-signed-in, all data restored after sign-in.
- [ ] **APNs production push test** — Send push to device running the production (not dev) build. Verify arrival, payload, and deep-link from cold start.
- [ ] **Biometric sign-in** — Face ID / Touch ID works on real device, falls back to password, can be disabled in Settings.
- [ ] **Offline walkthrough** — Set airplane mode, walk every major tab. Every screen should show an honest "No connection" message with retry (global banner exists; verify per-screen states).

---

## 🧪 Deferred / Nice-to-Have

- [ ] **State restoration** — App always cold-starts at Home tab (no @SceneStorage). Deferred: adding tab + scroll-position persistence is low-risk UX improvement but needs careful state-management testing.
  - **WHERE**: `AMENAPP/ContentView.swift` — add `@SceneStorage("selectedTab") private var selectedTab`.

- [ ] **GDPR/CCPA data export** — "Download My Data" button now shows a confirmation toast (agent fix). The actual backend CF needs to be built to complete GDPR/CCPA compliance. See Cloud Functions section above.

- [ ] **Force-update gate** — Verify the minimum-version gate (Remote Config) actually blocks old builds from reaching the main feed, and that feature kill-switches can toggle without a new release.

- [ ] **Color token sweep** — ~1,661 remaining `Color.white`/`Color.black` instances; most are intentional (media overlays, dark gradient cards). Run a targeted sweep of non-media surfaces if dark mode parity is a launch requirement.

- [ ] **Shadow standardization** — ~1,861 `.shadow(...)` calls. Lower priority; no UX regression risk.
